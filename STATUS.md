# STATUS — живой журнал команды

> Единственный источник правды о том, кто что сделал. Тезисно. Подробная история — в git-логе.
> Сжат 2026-07-08 по общему согласию (было 551 строка поэтапного лога).

## Правила
1. **Начало сессии:** `git pull origin main`, дать Claude прочитать этот файл.
2. **Конец блока:** правишь **только свою** секцию (владение из TEAM_PLAN). Коммит `status: роль N` — **можно сразу в main без PR** (журнал, секции не пересекаются). Пуш отбило → `git pull --rebase origin main`.

---

## 🟢 Текущее состояние системы (MVP собран)
- **Бот ЖИВОЙ в Telegram** (@pocket_law_ai_bot) в docker, реальный агент, egress через xray-прокси Роли 4, `STORAGE_BACKEND=postgres`. И0–И3 закрыты.
- **Данные:** `law_articles_dev` = **13770 чанков / 35 актов** (ТК 623, УК 608, ГК ч.1-4 1846, СК 183, ЗоЗПП 75, КоАП 1502, ЖК 394, НК ч.1-2 2797, ГПК 523, УПК 678, ЗК РФ 382, КАС РФ 503, Закон об исполнительном производстве 236, Закон о банкротстве 1033, ОСАГО 106, Закон о потребкредите 86, Закон об ипотеке 149, Закон о долевом строительстве 130, Закон о персональных данных 54, Закон о регистрации недвижимости 349, Закон о садоводстве 91, Закон об актах гражданского состояния 113, Закон о страховых пенсиях 105, Закон об охране здоровья 199, Закон об ОМС 127, Закон о свободе передвижения 19, Закон об образовании 328, Закон о положении иностранных граждан 210, Закон об обращениях граждан 20, Закон о взыскании задолженности 51, Закон о материнском капитале 38, Закон об опеке и попечительстве 40, Закон о занятости населения 105, Закон о пособиях на детей 45, Закон о приватизации жилья 22) / 1024-мер (`deepvk/USER-bge-m3`), `source_url` в payload → кликабельные цитаты.
- **LLM:** Polza.ai, `google/gemini-2.5-flash-lite` (быстрая/дешёвая), абстрагирована (смена = `LLM_MODEL`), ретраи на 503.
- **Итоговые метрики к защите (2026-07-09, боевой стек, HyDE вкл):** сквозной Recall@цитаты ТК **98% (61/62)** (HyDE поднял с 95%); мультикодекс routing purity **100%** (книжные 28/28 + житейские 35/35); житейский recall@ретрив **91–94%** (LLM-вариативность); контроль вне права 1/1; anti-hallucination подтверждён.
- **Метрики на РАСШИРЕННОЙ базе (ТК 623 + СК 183 + ЗоЗПП 75, 2026-07-08):** Recall@цитаты **ТК 62/62, СК 10/10, ЗоЗПП 10/10 = 82/82 (100%)**; чистота роутинга **100%** — ни одной чужой статьи в цитатах. Регрессии ТК от мультикодекса нет (СК/ЗоЗПП — по 10 «книжных» кейсов, не разговорных).
- **Роутинг провалидирован на ПОЛНОЙ базе Tier-2 (10 кодексов, 9229 точек, 2026-07-08):** после боевого прогона Роли 4 прогнал сквозную проверку по УК/ГК/КоАП/ЖК/НК/ГПК/УПК (+ТК/СК): **11/12 вопросов → строго свой кодекс, ни одного ошибочного перетекания**; 12-й («раздел имущества») корректно ушёл в стык СК+ГК. Строки `act` = «ГК РФ»/«НК РФ» (4 части ГК и 2 части НК под одним act) подтверждены по payload — `BRANCH_TO_ACTS` совпал без правок. Готовы к новым кодексам сразу.
- **Управление документами — НАША часть готова (ответ на предложение Роли 1):** `agent/documents.py` — `list_user_documents(user_id)` (агрегат по `doc_id`, свежие сверху, имя+дата+чанки) и `delete_user_documents(user_id, doc_id=None)` (прямой Qdrant; `None`=все). **`delete` закрывает пробел 152-ФЗ:** `/delete` бота чистил только Postgres, файлы оставались в векторной базе. `ingest` теперь кладёт `filename` в payload (для списка/выбора). Методы на `Agent`. 41 тест + живой e2e на сервере (2 загрузки→список с именами→delete одного→delete всех). В `Roman_SPT`.
  - **Контракт ×4 (shared/contracts.py) — предлагаю ратифицировать сигнатуры:** `list_user_documents(user_id)->list[UserDocument]`, `delete_user_documents(user_id, doc_id=None)`, `ingest_document(..., filename=None)`, `answer_question(..., doc_ids=None)` + модель `UserDocument(doc_id, filename, uploaded_at, chunks)` + поле `doc_id` в `RetrievedChunk`. Реализация у нас уже есть (кроме scope) — PR оформим на четверых.
  - ~~**Роль 4 (для скоупа поиска по документу):**~~ **СДЕЛАНО (2026-07-08):** `search_law(query, user_id, acts=None, doc_ids=None)` — серверный фильтр `user_documents` по `doc_id` (`MatchAny`, тот же паттерн, что `acts`); `RetrievedChunk.doc_id` проброшен из payload. Контракт ×4 ратифицирован в `shared/contracts.py` (`UserDocument`, `list_user_documents`, `delete_user_documents`, `ingest_document(filename)`, `answer_question(doc_ids)` — сигнатуры 1-в-1 с вашей реализацией). Payload-индекс `user_id`(int)/`doc_id`(keyword) на `user_documents`. **Живой e2e на сервере:** 2 документа с именами → список → поиск со скоупом на один doc_id (нашёл только его) → delete одного (второй цел) → delete всех. Можно подключать `doc_ids` в `retrieve`/state.
  - ~~**Скоуп поиска по документу — подключить `doc_ids` в retrieve/state (Роль 2)**~~ **СДЕЛАНО (PR #27, смержен, 2026-07-08):** `answer_question(user_id, text, doc_ids=None)` -> `initial_state` -> `state["doc_ids"]` -> `retrieve` пробрасывает в `search_law` (серверный фильтр). Пусто/None = по всем документам. 46 тестов + живой e2e (2 договора 50000/90000 -> скоуп на док-1 вернул только «Договор №1», на док-2 только «Договор №2», без скоупа — оба). **Роль 1: для активного скоупа пикера — `agent.answer_question(user_id, text, doc_ids=[...])`.**
  - **Роль 1 (UI, ваша зона):** передавать имя файла в `ingest_document(..., filename=<имя из Telegram/caption>)`; `/delete` → звать `agent.delete_user_documents(user_id)` (полная очистка); для выбора — `agent.list_user_documents(user_id)`. По идее пользователя: reply-кнопка «📄 Документы» при ≥2 файлах → inline-пикер [док…][Все], хранить активный скоуп per-user, под ответом подпись «🔎 по документу: <имя>». Батч/агрегацию альбома по `media_group_id` — тоже на вашей стороне.
- **Tier-2 (2026-07-08): +11 актов (УК/ГК ч.1-4/КоАП/ЖК/НК ч.1-2/ГПК/УПК), боевой прогон на сервере завершён.** `search_law(acts=[...])` точечно проверен на новых кодексах: кража→только УК РФ, наследство→только ГК РФ, штраф ГИБДД→только КоАП РФ (фильтр строгий, перетекания между актами нет). Полный Recall@цитаты по всем 10 кодексам ещё не прогнан (это на Ролях 2/3).

### Открытые хвосты (не блокируют MVP)
- **Роль 4:** Branch protection на `main` (задача 10). Постоянное egress-решение вместо личного VPN.
- ~~**Роль 2 (UX — от Роли 1):** off-topic → переспрос про сферу права~~ **СДЕЛАНО** (2026-07-08): не-юр вопрос (погода/рецепт/код) → мягкий отказ по области (`make_offtopic`, refused=True); пустой ввод → уточнение. Заодно **расширил `is_legal`** в intent (развязал с branches) — реальные юр-вопросы (возврат товара, развод, сосед, штраф) больше не отшиваются как «не моя тема»: `is_legal=True` + верный роутинг по кодексу, честный отказ «нет норм в базе» до заливки соотв. кодекса. Проверено вживую. В `Roman_SPT`.
- **Пользователь:** лимит трат на аккаунте Polza (только владелец ключа).
- ~~**🔴 Роль 2 (от Роли 1, по живому тесту 2026-07-09) — слабые/некорректные ответы, не бот:** (1) вопрос по загруженному документу со скоупом возвращал нерелевантные ГК-статьи вместо разбора из содержимого; (2) ипотека/кредит роутились в ГК вместо спец-законов.~~ **ЗАКРЫТО Ролью 2 (2026-07-10):** (1) — «Баг B закрыт + бот-консультант» (документ ведёт разбор, `fetch_document_text`+`COMPOSE_DOC_SYSTEM`, см. секцию Роли 2); (2) — «Баг A закрыт» (keyword-net на ипотеку/осаго/микрозайм + round-robin по актам). Оба в проде после сегодняшних пересборок.
- ~~**💡 Предложение (от Роли 1) — управление документами**~~ **РЕАЛИЗОВАНО end-to-end (2026-07-08).** Инициатива Роли 1 → контракт ×4 → Роль 2 (`list/delete/filename/doc_ids` scope, PR #25/#27) → Роль 4 (`search_law(doc_ids)`) → бот (Роль 1): `/documents` со списком и inline-пикером, липкий скоуп по документу, `/delete` c purge `user_documents` (закрыт пробел 152-ФЗ), — PR #26/#28. Полный цикл список/скоуп/удаление/**агрегация альбома** (PR #29: файлы одного `media_group_id` → одно сводное «принято N») работает. Детали — в секциях Ролей 1 и 2. **Тема документов закрыта полностью, остатков нет.**
- **Фаза 2 (вне MVP):** LLM query expansion уже сделан; тезаурус/граф статей, другие кодексы, `law_versions`.

---

## Роль 1 · Bot Layer (`bot/`) — ветка `Dordzhi-Msk` · **DoD закрыт**
- aiogram 3.x (long polling). Команды: `/start` (обяз. согласие 152-ФЗ, без него бот молчит), `/help`, `/documents` (список загруженного + пикер скоупа), `/delete` (удаление данных + документов). FSM + 3 ветки контракта (ответ с цитатой / уточнение / отказ).
- Приём **PDF/фото** (валидация по magic-bytes, ≤20 МБ) → `ingest_document`; **детект URL** → `ingest_url`; ссылка+вопрос в одном сообщении.
- Форматтер 3.5: MarkdownV2 с экранированием, «Основание: ст. N», дисклеймер, разбивка >4096, кликабельные `source_url`, мягкая деградация на плохой разметке.
- **`PostgresRepository`** (`STORAGE_BACKEND=postgres`): согласие/история/документы переживают рестарт (критично для 152-ФЗ); rate-limit поверх `dialog_history`; глобальный `@dp.errors`; edited_message; идемпотентное согласие.
- Бот подтверждён live в Telegram на реальном агенте. Правок под баг «документ не доходил до ответа» в `bot/` не потребовалось (баг был в агенте).
- После пересборки образа (PR #21) сценарий документ→ответ **подтверждён в живом Telegram**: ссылка ст.115 → «28 календарных дней» (ст.115/267/322); полный ТК → вопрос про увольнение → grounded-ответ (ст.81/180/296/318/394); **загрузка PDF-договора** → «какой оклад?» → «120 000 ₽» (число есть только в файле, не в ТК → доказано, что документ прочитан) + ст.57. Документ реально влияет на ответ, цитаты кликабельны. **Все задачи Роли 1 закрыты.**
- **UX-фикс (PR #22, смержен):** вопрос, посланный ДО «документ принят», при конкурентной обработке апдейтов уходил в агента раньше индексации → ответ без контекста. Теперь на время приёма юзер помечается в `_ingesting`, а `on_question`/`on_edited_question` отвечают мягкой подсказкой «⏳ обрабатываю документ, задайте вопрос после подтверждения» (агент не зовётся); метка снимается в `finally`. +3 теста. **Live** — Роль 4 пересобрала образ (08:20), гейт подтверждён в контейнере.
- **Гейт: ref-count вместо set (PR #24, смержен).** При загрузке нескольких файлов разом (альбом = отдельные апдейты → параллельные `on_file`) `set` снимал метку после первого же приёма, пока остальные грузятся. Заменил на счётчик (`_ingest_begin/_ingest_end/_is_ingesting`): метка снимается только после последнего приёма, ключ вычищается. +1 тест (26 passed). Роль 4: подхватится следующей пересборкой образа.
- **Управление документами — бот-часть готова (PR #26, смержен; ответ на нашу же инициативу + запрос Роли 2).** Подключил готовые методы Роли 2: (1) `on_file`/`on_url` прокидывают `filename` (имя файла / URL) в `ingest_*`; (2) новая команда **`/documents`** — список загруженного через `agent.list_user_documents` (обычный текст, имена не в MarkdownV2); (3) **`/delete` теперь чистит и `user_documents` в Qdrant** через `agent.delete_user_documents` — **закрыт пробел 152-ФЗ на стороне бота** (раньше удалялся только Postgres), best-effort. `AgentClient`/`MockAgent` расширены, `/help`+меню обновлены. +5 тестов (31 passed). Роль 4: подхватится пересборкой образа.
  - **Роль 2:** все ваши запросы по управлению документами закрыты. filename / `/delete`-purge / `/documents` — PR #26. **Скоуп «искать по документу N» — PR #28 (смержен):** `/documents` рисует inline-пикер (кнопка на документ + «искать по всем», активный ✓), выбор липкий per-user, `_answer_question` шлёт `doc_ids=[…]` в `agent.answer_question`, под ответом подпись «🔎 по документу: <имя>»; `/delete` и удалённый `doc_id` сбрасывают скоуп. +4 теста (35 passed). **Итого управление документами (список/скоуп/удаление/152-ФЗ/агрегация альбома PR #29) — готово end-to-end, остатков нет.**
- **LIVE (2026-07-08):** Роль 4 пересобрала образ; весь bot-стек документов (PR #24/#26/#28/#29) подтверждён в живом контейнере — `grep` маркеров (`cmd_documents`, `delete_user_documents`, `on_scope_select`, `_album_add`, `_ingest_begin`) в `/app` = все на месте, 0 рестартов.
- **Самопроверка хендлеров против РЕАЛЬНОГО агента (без Telegram-транспорта, 2026-07-08):** прогнал `on_file`-ingest / `cmd_documents` / `on_scope_select` / `on_question` / `cmd_delete` на сервере через настоящий `Agent()` на двух договорах (A=оклад 111000, B=222000). **Скоуп реально различает документы:** вопрос «какой оклад?» со скоупом на A → «111 000», на B → «222 000» (эти числа только в своих файлах); `/documents` показал оба с именами/датами; подпись «🔎 по документу: <имя>» на месте; `/delete` → 0 документов. Не покрыто мной (нужен Telegram): тап по inline-кнопке, доставка альбома, `bot.download` — на ручную проверку. Тестовые данные из прод-Qdrant удалены.
- **Самоаудит зоны (PR #34, 2026-07-09):** прошёл весь `bot/`, нашёл 2 реальных недочёта (не баги MVP). **Н1** — `_flush_album` в detached-задаче без обёртки: сбой отправки сводки альбома (сеть/удалённое сообщение) уходил в «Task exception was never retrieved», `@dp.errors` его не ловит → try/except+log. **Н2** — залипшая ✓ в пикере `/documents`: тап по документу менял скоуп, но галочка не двигалась до переоткрытия списка → `_refresh_picker`→`edit_reply_markup` (best-effort, lazy-import без цикла). +2 теста (41 passed). Осознанно НЕ трогал (не алармы): двойной учёт rate-limit на «ссылка+вопрос» (by-design), `on_scope_select` без consent-мидлвари (недостижимо без документов). Роль 4: подхватится следующей пересборкой образа.
- **Скоуп-UX по живому тесту (PR #38, 2026-07-09):** выбранный документ «залипал», снять выбор было неочевидно (казалось, помогает только `/delete`). Исправил: **`/all`** — снять выбор (команда в меню + `/help`); **новый файл/ссылка авто-сбрасывает прежний липкий скоуп** (не липнет к старому); **ссылка+вопрос** отвечается по только что загруженному документу (`scope_override`, не по старому скоупу); в `/documents` кнопка сброса явная «♻️ Сбросить выбор» + строка «Сейчас ищу только по: <имя>». +3 теста (44 passed). Наблюдение: **rate-limit обнуляется через `/delete`+`/start`** (лимит считается по `dialog_history`, а `/delete` её чистит) — по сути это законная реализация права на удаление (152-ФЗ), не фиксю. Роль 4: подхватится пересборкой образа.
- **Вопрос в подписи к файлу (PR #39, 2026-07-09):** одиночный PDF/фото с вопросом-подписью раньше только подтверждал приём — теперь сразу отвечает по этому документу (`scope_override`, как «ссылка+вопрос»); альбом пропускаем (подпись не про всю пачку). `/help` дополнен. +1 тест (45 passed). Роль 4: подхватится пересборкой образа.
- **Фикс рассинхрона заголовок↔✓ в /documents (PR #40, 2026-07-09):** по живому скрину — заголовок «Сейчас ищу по: X» показывал старый документ, а ✓ уже на другом (при тапе перерисовывалась только клавиатура, текст оставался). Регрессия на стыке PR #34 (перерисовка ✓) и #38 (заголовок). Единый `_documents_view` (текст+клавиатура из одного источника), `_refresh_picker` перерисовывает всё сообщение (`edit_text`). 45 passed. **Образ #38 УЖЕ пересобран и в проде** (подтверждено скрином: заголовок-скоуп + кнопка «♻️ Сбросить выбор» + `/all` живые); #39/#40 ждут следующей пересборки.
- **Мультивыбор документов в /documents (PR #41, 2026-07-09):** по живому тесту — отмечался только ОДИН документ («либо все, либо один»), хотя агент/`search_law` принимают список `doc_ids` (MatchAny). Сделал мультивыбор в боте: скоуп теперь набор `{doc_id: имя}`, тап переключает отметку (✓ на всех отмеченных), «Искать по всем»/`/all` снимают все, поиск идёт по всем отмеченным (`doc_ids=[…]`), заголовок/подпись — «по N документам: …», удалённый документ убирается из выборки. +2 теста (46 passed). Бэкенд не трогал — уже поддерживает.
- **Кнопка «Отметить все» в /documents (PR #42, 2026-07-09):** симметрично «Сбросить все» — «☑️ Отметить все» (отметить все документы разом, когда их много); показывается, когда отмечены не все. +1 тест (47 passed).
- **Убрал команду /all (PR #43, 2026-07-09):** по замечанию — `/all` дублировал кнопку «♻️ Сбросить отметки» в `/documents`, не плодим команды. Вся логика скоупа теперь в `/documents` (отметить один/несколько/все, сбросить — кнопками); подпись под ответом ведёт на `/documents`. Внутренний callback `scope:all` (кнопка сброса) остался. 46 passed.
- **Экспорт ответа в «памятку» (PR #44 → #47, 2026-07-09):** под содержательным ответом — кнопка «📄 Скачать с источниками» → **PDF** с вопросом, ответом, «Основание: ст. N … + url», дисклеймером (сначала был `.md`, но сырые `#/**` не читабельны юзеру → перешёл на PDF; `.txt` — фолбэк). **Рендер через PyMuPDF (уже зависимость проекта) встроенным шрифтом `helv` — БЕЗ новых зависимостей и без встраивания TTF**, кириллица извлекается из PDF 1:1 (round-trip тест). Оформлено как СПРАВКА, не заключение (нейтральный заголовок, «не является юр-консультацией и не является офиц. документом» — по разбору юр-рисков). Без новых LLM-вызовов; храним последний содержательный ответ per-user, чистим при /delete; отказ/уточнение не экспортируем.
- **Онбординг /topics (PR #45, 2026-07-09):** кнопки-темы (трудовые/семейные/потребитель/жильё/штрафы/кредиты) → частые вопросы → тап прогоняется через обычный ответ. Снижает порог входа (пустое поле), ведёт в сильные зоны базы. Кнопка «💡 Частые темы» под приветствием/согласием. Гейты consent/rate-limit повторены в callback-хендлере (идёт мимо middleware). +5 тестов (56 passed). Контракт не трогается.
- **📋 План (голосовой ввод) — на будущее, НЕ реализовано:** голосовое → STT (распознавание речи) → обычный поток вопроса. Хендлер `F.voice` + скачивание файла — моя зона (`bot/`); OGG/Opus от Telegram большинство STT едят напрямую. Два варианта реализации STT:
  - **Вариант A — облачный STT (рекомендую для диплома).** Кандидаты: OpenAI Whisper API (~$0.006/мин ≈ 0.5 ₽/мин), **Yandex SpeechKit** (лучший по русскому, РФ-хостинг = 152-ФЗ-дружелюбно), Google STT. Затраты — доли рубля за вопрос (для демо фактически бесплатно). Сложность **низкая** (~полдня): скачать voice → один HTTP-POST на `/audio/transcriptions` → текст → `_answer_question`. Блокер — **есть ли STT-канал: проверить, отдаёт ли наш провайдер Polza `/audio/transcriptions`** (если да — drop-in на том же ключе); если нет — отдельный ключ Yandex/OpenAI (Роль 4, `.env`, config).
  - **Вариант B — локальный self-hosted STT на сервере.** `faster-whisper` / `whisper.cpp`, модель small/medium (~1–3 ГБ). Цена за запрос — ноль, но **+RAM/CPU** (сервер уже тянет qdrant+postgres+bot+эмбеддинги bge-m3; GPU, видимо, нет → распознавание на CPU медленное) + нужен **ffmpeg** для декода OGG. Сложность **средняя**, инфра тяжелее. Плюс — голос не покидает сервер (152-ФЗ идеально). Зона — Роль 4 (зависимость/модель/инфра).
  - **UX (общее):** показывать распознанный текст обратно («🎙 Вы спросили: …») для контроля, chat-action «распознаю…», лимит длины голосового (≤60 сек).
- **📋 Прочие идеи-преимущества над ИИ-чатом (обсуждены, не реализованы):** дословный текст статьи в ответе (у нас есть retrieved-чанк), проверка загруженного документа на противоречия закону, вывод confidence юзеру (у Роли 2 уже пишется `confidence_log`) — все три требуют доп. строки в контракте `Answer` (PR ×4). UX-блок (голос) — моя зона по хендлеру.
- **Авто-скоуп на свежезагруженный документ (PR #48, 2026-07-10) — ответ на координацию Роли 2.** Их doc-разбор по чанкам включается только при `doc_ids`, а бот после загрузки файла БЕЗ подписи лишь СБРАСЫВАЛ скоуп → следующий вопрос шёл без `doc_ids` и разбор не срабатывал. Теперь загрузка **авто-выбирает** документ (замена прежнего скоупа): одиночный файл → скоуп={он}; ссылка → скоуп={документ по ссылке}; альбом → скоуп={все документы альбома} (на flush сводки). Индикатор «🔎 Сейчас ищу только по…» и сброс в `/documents` уже есть. +1 тест, 2 обновлены (58 passed). **Координация Роли 2 по авто-скоупу закрыта.**
- **Юр-тест по документу с нарушениями (2026-07-10):** сгенерировал эталонный PDF (трудовой договор, **8 заложенных нарушений ТК**) и прогнал live. **Бот-слой чист** — приём документа, авто-скоуп, рендер ответа, кликабельные цитаты, дисклеймер на месте, 0 дефектов в `bot/`. Разобрал ответ агента, нашёл слабые места → Роли 2. Главная находка — **рассинхрон «Основание↔текст»**: в блоке источников висели неиспользованные ст.58/74/84, а реально задействованные (115/133/136/153) выпадали. **Роль 2 уже починила** (мерж `f926343` «выравнивание цитат — разбор Роли 1», в проде после сегодняшней пересборки). Остаток (пропуск МРОТ ст.133/ст.136, ст.92 vs 91, ст.71 vs 80) Роль 2 приняла и отложила отдельным заходом (её секция). PDF-кейс воспроизводимый — держу для регресса.
- **Фикс потери контекста при уточнении (PR #49, 2026-07-10, по живому тесту).** Баг: бот переспрашивает → пользователь уточняет → бот терял контекст и обрабатывал уточнение как **новый** вопрос. Причина — агент безстейтовый (`answer_question` строит `initial_state` с нуля, историю не читает), а бот при `clarify` ставил `awaiting_clarification`, но **исходный вопрос не сохранял** → `on_question` слал агенту только текст уточнения («да»/«трудовой»). Фикс (только `bot/`, контракт не тронут): при переспросе кладём в FSM `pending_question` + сам переспрос агента (в нём часто варианты «трудовой или ГПХ?»); `_with_clarification_context` склеивает исходный вопрос + переспрос + ответ и отдаёт агенту единым текстом; вне режима уточнения — как есть. +2 теста (`test_clarify`), фейки `state` в 7 тестах дополнены `get_state`/`update_data`. **60 passed.** Роль 4: подхватится следующей пересборкой.
- **Рендер дословной выдержки статьи (PR #50, 2026-07-10) — закрыл контракт 2б со своей стороны.** Команда согласовала `Citation.text` (короткая выдержка из статьи, из ретрива; комментарий контракта: «заполняет Роль 2/verify, **рендерит Роль 1**»). Реализовал рендер: в Telegram под цитатой — выдержка курсивом `_«…»_` (усечение по словам, лимит 220, MarkdownV2-экранирование), в памятке .txt/PDF — полнее (лимит 500). Поле опционально → нет `text` = вывод как раньше (обратная совместимость). Прогнал реальный рендер: TG-сообщение + PDF round-trip (выдержка извлекается 1:1). +5 тестов, **65 passed**. Роль 4: подхватится следующей пересборкой (Роль 2 уже отдаёт `text` — фича включится, как только образ соберётся из свежего main).
- **Защита кнопки экспорта от спама (PR #51, 2026-07-10, по живому тесту).** Кнопку «Скачать с источниками» можно было жать повторно — каждый тап генерил и слал новый PDF (дубликаты в чате). Фикс: резервируем ключ `(chat_id, message_id)` до отправки (гасит двойной тап), после успешной выгрузки убираем кнопку с сообщения (`edit_reply_markup=None`); на ошибке отправки резерв снимаем (повтор разрешён). +2 теста, **67 passed**. Роль 4: следующей пересборкой.
- **Пересборка #39–#51: едет в прод с сегодняшними ребилдами.** Роль 4 сегодня (2026-07-10) пересобрала образ несколько раз **из `main`** (фиксы Роли 2 INSUFFICIENT/doc-фрод/цитаты + полный переиндекс 39 актов). Так как сборка идёт из `main`, куда #47 (PDF-экспорт) и #48 (авто-скоуп) уже влиты, они в образ попадают. **Grep-подтверждения бот-маркеров** (`format_export_pdf`, `_scope_set_only`) в контейнере из этой сессии у меня нет (нет SSH-доступа к серверу — баннер молчит) → прошу Роль 4 при случае подтвердить и снять этот пункт.

## Роль 2 · Agent Orchestrator (`agent/`) — ветка `Roman_SPT` · **функционально завершена**
- Граф `intent → retrieve → verify → {compose|clarify|refuse|offtopic}`, API 3.1 (`Agent()` — drop-in). LLM абстрагирован (Polza.ai, `gemini-2.5-flash-lite`, ретраи на 503). Бюджет: max_tokens=900, recursion=8, обрезка 4000, guard пустого ввода.
- **Anti-hallucination:** цитаты только из `verify`; нет норм → INSUFFICIENT/refuse; не-юр → offtopic. **Безопасность промптов** (инъекции через файл/URL, социнженерия «создатель/президент») — проверено на живом LLM.
- **Документы:** `ingest_document` (PyMuPDF+OCR rus), `ingest_url` (SSRF-защита), изоляция по `user_id`; управление `list/delete_user_documents`+`filename` и **скоуп по doc_id** (`answer_question(doc_ids)`, PR #25/#27); `delete` закрыл пробел 152-ФЗ. Переформулировка «житейский→юридический» в intent.
- **Мультикодекс (PR #23):** `intent`→branches→`config.BRANCH_TO_ACTS`→серверный `search_law(acts)` Роли 4 (10 кодексов, MatchAny до top-K; не уверен→по всем). `is_legal` расширен (развязан с branches). Роутинг на боевой базе — без перетекания между кодексами.
- **Ретрив 71%→94% на житейских (PR #30/#31):** квота top-K на кодекс + номерной fast-path («ст.158 УК») + **HyDE-фьюжн** (флаг `AGENT_HYDE`, генерится параллельно intent — латентность не растёт, сбой не роняет ответ). Живой e2e: НК-вычет→ст.220, ГПК-оспорить→ст.320.
- **confidence-телеметрия (PR #32):** пишется на всех юр-исходах (успех+INSUFFICIENT+refuse — сигнал качества); `confidence_log` в проде живёт (444+ строки).
- **Метрики к защите (2026-07-09, HyDE вкл):** ТК сквозной Recall@цитаты **98% (61/62)** (HyDE поднял с 95%) + мультикодекс routing purity **100%** (28/28 книжн. + 35/35 житейск.) + житейский recall@ретрив **91–94%** (LLM-вариативность) + контроль вне права 1/1; anti-hallucination подтверждён. 57 тестов.
- **Роутинг 15→35 отраслей (PR #33/#35, дизайн по A/B):** +5 актов Tier-3 (ЗК/КАС/229-ФЗ/127-ФЗ/ОСАГО; **сквозной e2e на проде 10/10** — «штраф без ОСАГО»→КоАП верно, «кадастр»→КАС+ЗК стык) и +20 бытовых ФЗ (кредиты…приватизация). **A/B:** плоский 1:1 бьёт группировку **96%/84%**, длинные имена бьют короткие **100%/95%** («дачи» роняло садоводство); большая выборка **46/46=100%**. Act-строки Роли 3 подтверждены ×2 (реиндекс не нужен). 20 ФЗ инертны до заливки Ролью 4.
- **Пакет-харденинг (PR #36, аудит роли):** fast-path алиасы ЗК/КАС; канарейка синхронности `BRANCH_TO_ACTS`↔`INTENT_SYSTEM` (ловит дрейф при добавлении актов); потолок fan-out квоты `MAX_QUOTA_ACTS=4`. **+ PR #37: список веток в промпте автогенерируется из `BRANCH_TO_ACTS`** — единый источник правды, при добавлении акта правится одно место (регрессия 46/46=100%). 62 теста.
- **Баг A закрыт (keyword-net + round-robin, мерж в main напрямую — gh/PR-тулинга под рукой не было):** на длинном зашумлённом вопросе intent ронял вторичный спецзакон («разделить квартиру **в ипотеке**, ...развод..., покусала собака» → СК/ГК/ЖК, банк проигнорирован — репро юзера). Причина: LLM цепляется за салиентную тему; даже когда акт извлечён квотой, глобальный score-сорт топит его ниже `MAX_CITATIONS`. Фикс — (1) **keyword-net**: однозначный термин (ипотека/осаго/микрозайм/маткапитал/коллектор) в сыром вопросе форс-добавляет спецзакон в приоритет; (2) **round-robin по актам** в retrieve при ≥2 актах (топ-1 каждого акта в верхушку — завершает цель квоты: гарантия не только извлечения, но и места в цитатах). Одноактный роутинг/fast-path не трогаются. **A/B на проде:** вопрос юзера «Закон об ипотеке» в цитатах **1/6→6/6**, роутинг шумных **16/20→20/20**, ТК recall **8/8** и стык-чистота **100%** без регресса. +4 теста (66). ⚠️ Первый флажок Роли 1 по ипотеке был артефактом окна заливки данных; на **зашумлённых** формулировках баг реальный — его и чиним. **Хвост B (скоуп по документу) — в работе.**
- **Баг B закрыт + бот стал консультантом (мерж в main напрямую):** вопрос по ПРИСЛАННОМУ документу (письмо банка/налоговой, уведомление о сносе, претензия) роутился в мимо-закон или отшивался (refuse/offtopic) — intent видел лишь короткий вопрос («что это?»), а суть в самом документе игнорировалась. Фикс — **документ ВЕДЁТ разбор:** `fetch_document_text` подгружает текст → intent классифицирует отрасль и строит запрос к закону по содержимому → `route_after_verify` пускает разбор в compose даже без law-цитат → `COMPOSE_DOC_SYSTEM` (что это / что хотят / по закону / что делать / ⚠️ ОСТОРОЖНОЕ предупреждение о вероятности мошенничества, без обвинений). **A/B на проде:** снос БЫЛ refuse → стал ЗК 56.12 / ГК 279; мошенническое письмо БЫЛО offtopic → консультация + антифрод; трудовой договор ГК-generic → ТК. **Идея (бот-консультант):** `COMPOSE_SYSTEM` теперь даёт структурный ответ — суть → 1-2 практических шага (зафиксировать / претензия / заявление / орган / суд) → мягкий уточняющий нудж В ТЕКСТЕ (FSM-поле `clarifying_question` не трогаю — это UI Роли 1), компактно (~10 предложений), anti-hallucination и INSUFFICIENT целы. A/B на 5 бытовых: recall не просел, шаги 5/5, нудж только где нужен. +5 тестов (71).
- **Метрика расширена с ТК на всю базу (закрыт перекос):** собрал мультикодексный gold-набор `tests/legal_cases/multicode_eval.json` (57 бытовых вопросов по 10 кодексам + ФЗ, метка = множество приемлемых статей). Прогон на новом коде: **Recall@цитаты 54/57 = 95%** (УК/ГК/СК/ЖК 6/6, НК/ГПК 5/5, ТК/КоАП/ЗоЗПП 5/6, ФЗ 5/5) + **doc-консультация 3/3** (договор→ТК, снос→ЗК 56.12/ГК 279, мошенники→взыскание/УК + антифрод). Раньше цифра держалась на одном ТК. 3 промаха разобраны: ТК-81 (флагманский тяжёлый кейс, ретрив-вариативность — текст верный, статья не в чипах), КоАП-12.9 (реальный, копнуть роутинг/ретрив), ЗоЗПП-20 (узкая метка — дал соседние 18/19). ⚠️ ожидаемые статьи — черновик Роли 2, нужна лёгкая сверка с Ролью 3 (у неё исходники).
- **Самоаудит зоны (2026-07-09 вечер, по образцу PR #34 Роли 1):** прошёл весь `agent/`, 7 находок — все исправлены и провалидированы вживую. Главные: **Н1** — омоним «коллектор» (сантехника) ложно тянул Закон о взыскании, а round-robin гарантировал мусорной цитате место → триггер сужен до множ. числа/«коллекторск» (сантехника теперь → ЖК/ГК чисто); **Н2** — doc-разбор видел top-3 чанка, похожих на вопрос, а не документ по порядку → многочанковое письмо разбиралось частично; теперь compose разбирает упорядоченную голову (`state.doc_text`) — на живом 2-чанковом требовании банка в ответе ВСЕ факты шапки (банк/№ договора/сумма) + срок из хвоста; **Н3** — guard на INSUFFICIENT-литерал в doc-режиме; **Н4** — doc-ответ без markdown (ломался бы об MarkdownV2 бота) и короче (~12 предложений, 1500-2000 симв); **Н5** — кэш Qdrant-клиента в documents.py; **Н6** — ручка `AGENT_DOC_CONTEXT_CHARS`; **Н7** — стейл-докстринги. 17/17 тестов (73 всего). **⚠️ Роль 1 (координация):** файл БЕЗ подписи → ваш скоуп авто-сбрасывается, но не авто-устанавливается → следующий вопрос идёт без `doc_ids`, и наш doc-разбор не включается; рекомендуем авто-скоуп на свежезагруженный документ (индикатор «Сейчас ищу только по…» у вас уже есть).
- **Фикс утечки INSUFFICIENT (по живому скрину юзера, 2026-07-10):** на «коллекторы бросают мусор» модель вопреки промпту написала пояснение, а ПОТОМ `INSUFFICIENT` в конце — обе ветки compose ловили маркер только через `.startswith` → литерал «INSUFFICIENT» + нерелевантные цитаты (взыскание/ЖК/КоАП) улетали пользователю. Теперь `_is_insufficient` ловит маркер по границе слова ГДЕ УГОДНО (обычная и doc-ветка). +1 тест. Образ пересобран Ролью 4 сегодня 06:38 UTC (весь мой agent-код уже live — подтверждено grep-маркерами в `/app`); **этот фикс поедет следующей пересборкой.**
- **Роль 4 — нужна пересборка образа:** фикс утечки INSUFFICIENT (мерж d576380, поверх уже задеплоенного A/B/doc/аудита) в живом боте появится только после `docker compose build bot && up -d bot`.
- **КоАП 12.9 — промах метрики РАСКОПАН до конца, причина в ДАННЫХ (→ Роль 3), не в агенте (2026-07-10):** «превысил скорость на 40 км/ч» → intent верный (ветка административное→КоАП), retrieve верный (**ст.12.9 первой, score 0.81**), но **verify справедливо выкидывает 12.9** — в payload `status='repealed'`. Причина: у чанка утратила силу лишь **ЧАСТЬ 1** («1. (Часть утратила силу — ФЗ 196-ФЗ от 23.07.2013)»), а части 2–7 (сами штрафы за скорость) ЖИВЫЕ. Парсер помечает `repealed` ВСЮ статью, увидев «утратила силу» в одной части → живая норма молча исчезает из цитат, compose честно отказывает по оставшемуся мусору. **Масштаб:** в базе 400 repealed-чанков; в КоАП 88, из них **~6 ложных** (12.9/3.2/7.2/8.12/27.17/12.21 — есть живые части), 82 — реальные bare-стабы. Тот же паттерн вероятен в других актах. **→ Роль 3:** помечать `repealed` только когда УТРАТИЛА СИЛУ ВСЯ статья (bare-стаб «Статья N. (Утратила силу…)»), а не когда мёртвая лишь часть/пункт живой статьи; прогнать скан по всем 400. Агент менять НЕ нужно (цитировать repealed нельзя by-design), eval-метку 12.9 оставляю — верна, зазеленеет после фикса данных. Диагностика — прямыми scroll-пробами по прод-Qdrant (текст 12.9 прочитан глазами).
- **→ Роль 4 (Selectel, 2026-07-10):** пользователь удалил в панели Selectel лишние роутеры. Проверил сервер: вход по SSH, egress/NAT и аккаунты dev1–4 (по ключу) целы, роутер подсети `192.168.0.0/24` / floating-IP `89.223.29.157` жив. Просьба: глянуть в панели, не осталось ли висящих ресурсов после удаления, и **не трогать этот роутер** (на нём и вход, и egress бота).
- **Сверка меток `multicode_eval.json` с прод-базой (2026-07-10):** прогнал все 103 метки-статьи через scroll по act+article_no+status — **102/103 указывают на реальные ДЕЙСТВУЮЩИЕ статьи**. Единственная дыра — КоАП 12.9 (repealed, см. выше, → Роль 3). Черновой набор Роли 2 структурно верен; юр-полнота приемлемых множеств — на лёгкой сверке с Ролью 3. Note в eval обновлён.
- **Gold-набор: применил рекомендацию Роли 3 (2026-07-10).** Роль 3 сверила multicode_eval.json со своими исходниками — метки чистые (57/57 существуют). По её совету расширил кейс «затягивают гарантийный ремонт» ЗоЗПП {20}→{18,19,20} (18/19 тоже уместны) — снимает мой мягкий промах ЗоЗПП-20. Про КоАП 12.9: Роль 3 починила ложный repealed в parse.py (~49 статей в КоАП), закроется после переиндекса --force Ролью 4 — агент не трогаю (verify перестанет ронять статью, как только статус станет active). Проекция метрики после переиндекса: ~56/57 (останется только ретрив-вариативный ТК-81).
- **2б Цитирование текста ФЗ — РЕАЛИЗОВАНО (идея команды, контракт согласован, 2026-07-10):** цитаты теперь несут дословную выдержку из статьи под «Основание» — пользователю не надо скроллить весь закон по ссылке. **КОНТРАКТ ×4:** shared/contracts.py — в Citation добавлено text: str|None=None (аддитивно, обратно совместимо; команда согласовала выносить цитаты). Моя часть: verify._snippet кладёт короткую выдержку (≤450 симв, обрезка по границе предложения) из текста ретрив-чанка — из РЕТРИВА, не от LLM (grounding, не выдумка). Едет через _align_citations (выравнивание сохраняет). Живой прогон: ст.115→«…28 календарных дней…», КоАП 12.8→текст статьи; 19/19 тестов (+2). **Роль 1: рендерить Citation.text под «Основание: ст. N» (act/article/source_url у вас уже есть).** Ручка AGENT_CITATION_TEXT_CHARS. Роль 4: подхватится пересборкой образа.
- **«Режим аудита договора» (детерминированный чеклист статей ТК) — прогнан LLM-судьёй, НЕ прошёл гейт, откатан (2026-07-10):** прототип за флагом AGENT_CONTRACT_CHECKLIST грузил в doc-режим фикс-набор статей ТК (133/136/91/115/153/70/137/192/9/80) по номеру. Первый замер выглядел «вау» (5.5→8.0/8), но **слепой контроль вскрыл утечку эталона** (судья видел ожидаемые номера). Слепо, 3 прогона: baseline стабильно 6.5/8, чеклист 6.5/6.5/8.0 (среднее 7.0) — прирост +0.5 при ничьих в 2/3 → НЕ «надёжно лучше». Регресс чист (письмо банка с флагом ON → ГК, трудовой чеклист не течёт). Откатал (retrieve.py = main). **Мораль: LLM-Judge окупился — поймал иллюзию, дал дисциплинированный вывод. Полноценный «режим аудита» (чеклист + check-промпт + разбор по пунктам) может сработать, но это отдельная большая фича, не твик.**
- **LLM-as-Judge — офлайн-оценка качества (идея тимлида #3, 2026-07-10):** agent/eval/judge.py — судья-LLM (temp=0, строгая JSON-рубрика: grounded/correct/citations_match/complete по 0-2, total 0-8, verdict+issues) семантически оценивает ответ. Заменяет хрупкие регэксп-метрики, которые тонули в шуме LLM (из-за них аудит договоров был неубедителен). ВАЛИДИРОВАН: хороший ответ 8/8, галлюцинация (ст.999/45 дней) 2/8 с разбором «статья 999 не существует», цитаты-мусор 3/8, расплывчатый 0/8 — различает; на 6 реальных кейсах сам подсветил флагманский слабый ТК-81 (цитаты не те). agent/eval/run_judge.py — раннер для A/B (агент → судья → сводка), модель судьи = JUDGE_MODEL (иначе LLM_MODEL). Оффлайн-инструмент, НЕ рантайм-звено (латентность/стоимость, судья не безгрешен). +1 тест. Образа не требует (не в графе агента).
- **OCR-галлюцинации на фото — фикс (разбор тимлида + жалоба пользователя, 2026-07-10):** воспроизвёл фактом: картинка-каракули → OCR даёт пару шумовых символов → проходит пустой-check → doc-режим СОЧИНЯЕТ несуществующий документ («свидетельство о госрегистрации акта гражданского состояния… ст.8 Закона об АГС» — всё выдумано, из 16 символов шума). Корень: нет гейта качества OCR + фото читалось без предобработки. Фикс: (1) ingest._has_meaningful_text — отклоняем OCR-результат с <4 словоподобными токенами (только used_ocr путь; текстовый слой PDF надёжен, не режем) → «пришлите фото чётче или PDF»; (2) parse_pdf._preprocess — ориентация по EXIF + грейскейл + автоконтраст + апскейл до 1600px перед OCR. A/B на сервере: каракули/шум/пусто → отклонены (галлюцинаций нет), реальный кириллический текст → 5/5 ключевых слов распознано (предобработка не ломает). +2 теста (16/16 через оверлей). **Роль 4: следующей пересборкой образа.**
- **Фикс рассинхрона «Основание↔текст» — выравнивание цитат (по разбору Роли 1, 2026-07-10):** Роль 1 нашла классический RAG-баг сборки цитат: блок «Основание» строился из топ-5 ретрива по score, а не из того, что модель реально использовала → в цитатах висел мусор (подтянутые-но-неиспользованные ст.71/307/305), реально задействованные (ст.91) выпадали из-за обрезки, плюс в doc-режиме модель цитировала статьи из своих знаний (негрунтованные). Фикс: verify отдаёт ВЕСЬ грунтованный набор (снял обрезку [:MAX_CITATIONS]), а compose._align_citations строит «Основание» = {статьи в прозе} ∩ {найдено ретривом} с обрезкой MAX_CITATIONS; проза без номеров (обычный режим) → прежний топ ретрива (фолбэк, без регресса). Негрунтованные номера в «Основание» не попадают by design (anti-hallucination усилен). A/B на проде (оверлей): договор Роли 1 → Основание [70,84,142,305,100], мусора 0, негрунтованного 0 (было 3 мусорные + выпавшая 91); 4 обычных вопроса без регресса. +3 юнит-теста (12/12 через оверлей). **Роль 4: следующей пересборкой.** Отложено по согласованию: recall МРОТ ст.133/136 (#1/#2) и качество прозы «номер-к-нарушению» (#3/#4) — отдельным заходом.
- **Doc-консультант — явный ответ на прямой вопрос о мошенничестве (2026-07-10):** нашёл при прогоне doc-режима: на «это мошенники?» бот давал консультацию, но саму тему обмана не закрывал (антифрод-пункт 5 срабатывал только при явных тревожных признаках В ДОКУМЕНТЕ, не на вопрос пользователя). Правка COMPOSE_DOC_SYSTEM п.5: если пользователь САМ спрашивает про мошенничество — отвечаем ПРЯМО и как о ВЕРОЯТНОСТИ, не обвиняя (нет признаков → «явных признаков не вижу» + совет проверить подлинность по официальному каналу; есть признаки → предупреждение о вероятности). A/B на проде (оверлей, 3 кейса): легит-письмо+«это мошенники?» → прямой успокаивающий ответ; скам-письмо → предупреждение о вероятности + защита (УК 159.1/187, «не переводите, не сообщайте коды»); ОБВИНЕНИЙ нет нигде. Известная побочка: на легит-письме БЕЗ вопроса про обман модель всё равно добавляет мягкий необвиняющий совет «проверьте подлинность» (безвредно/даже полезно, промптом до конца не гасится). Правка ТОЛЬКО текста промпта (код не тронут), 9/9 doc-тестов зелёные. Роль 4: поедет следующей пересборкой образа (вместе с фиксом INSUFFICIENT).
## Роль 3 · Data Pipeline (`pipeline/`) — ветка `Roma_MSK` · **DoD закрыт**

**Итог: 13 800 чанков — LIVE в проде** (после переиндекса Роли 4 от 2026-07-10; было 13 770 — переиндекс подтянул свежие редакции). Покрытие ≈85% бытовых юр-тем гражданина.

> **⚠️ Как правильно называть цифру актов на защите (39 и 35 — обе верны, единицы разные):**
> **39** = кодов пайплайна (`--act`, ключи `pipeline/acts.py`) · **35** = уникальных значений `act` в payload Qdrant.
> Разница в том, что многочастные кодексы залиты как **один** `act`: ГК ч.1–4 → `«ГК РФ»`, НК ч.1–2 → `«НК РФ»` (4+2 кода → 2 акта). Сверено по коду, не по памяти.
> Формулировка для слайда: **«35 законодательных актов (11 кодексов + 24 ФЗ), 39 источников-документов, 13 800 чанков»**.

**Пайплайн.** `fetch → parse → chunk → embed → upsert`, CLI `python -m pipeline.run --act <code>`. Источник — ИПС pravo.gov.ru (двухшаговый экспорт последней действующей редакции). Чанк = **статья** (длинные режутся по абзацам, лимит 3000 симв.; заголовок статьи дублируется в каждый чанк). Детерминированный `id = uuid5("{act}:{article}:{i}")` + версии по hash → идемпотентно, коллизий номеров между кодексами нет. `source_url` в payload → кликабельные цитаты. Обновление акта — delete-by-filter `act==X` + upsert.

**Архитектура хранения (решение).** ОДНА коллекция `law_articles` + роутинг по полю `act` (не коллекция-на-закон): «не уверен» → ищем по всем, нужный закон всплывает сам; стыки (ТК+СК) бесплатны. Межзаконная интерференция измерена — ничтожна (MRR −0.005).

**Выбор модели — бенчмарк (ключ к защите).** `deepvk/USER-bge-m3` (1024, без префикса): R@5 книжный **0.97** / живой **0.84**. **Reranker (bge-reranker-v2-m3) и гибрид BM25+dense измерены — оба НЕ помогают** (reranker ронял ст. 81; гибрид MRR 0.89→0.70) → чистый dense, обоснованный негативный результат. Юр-eval: метка = **множество** приемлемых статей. Наборы `eval_colloquial.json` (32 «живых») / `eval_extended.json` (138 «книжных») — в `pipeline/`.

**Инженерные победы — баги источника, пойманные и закрытые:**
- **Редакция по макс. ДАТЕ, а не max(rdk):** `rdk` не монотонен по дате (ГК ч.3: max(rdk) = исходная редакция 2001 г.) — без фикса залили бы право 20-летней давности.
- **Фетч больших и малых документов:** НК ч.2 (~9 МБ) валит savertf → фолбэк `doc_itself&fulltext=1`; полнота проверяется по числу статей в parse, а не байтовым порогом (иначе короткие законы <100 КБ ложно отвергались).
- **Парсер обобщён под все форматы ИПС:** голые стабы утративших силу статей ФЗ → `repealed`; двойная дефисная нумерация (`201.15-2-1`); старый формат «Закон РФ 1991» (заголовки статей без класса `H` → двухпроходный разбор). Регрессий на рабочих актах нет.

**Аудит актуальности (2026-07-09).** `_latest_redaction` по всем актам — редакции свежие; 2 устаревших в проде (ГК ч.4, ГПК) перезалиты Ролью 4 `--force`.

**Сверка gold-набора Роли 2 (2026-07-10).** Прогнал `multicode_eval.json` (57 кейсов) против наших исходников — **метки Роли 2 чистые (57/57 существуют)**. Заодно поймал наш баг: пометка «(Часть утратила силу)» у части статьи ложно помечала repealed ВСЮ статью — в КоАП так «умерли» **~49 действующих** (12.9 скорость, 3.2 виды наказаний, …). Фикс `parse.py`: repealed только если после удаления скобочных пометок не осталось осмысленного текста. Влияет на метаданные статуса, не на контент (`search_law` по status не фильтрует → чанки в индексе, находятся).

~~**🔧 РОЛЬ 4 — переиндекс ВСЕХ актов с `--force`**~~ → **✅ СДЕЛАНО Ролью 4 (2026-07-10):** 39/39 актов `exit=0`, ~4 часа. **КоАП 12.9 подтверждена `status=active`** — ложный `repealed` вычищен из прода. База 13 770 → **13 800**. Наш встроенный retry отработал: `502` от ИПС на `ip_229` самовосстановился, счёт сошёлся (236 чанков, орфанов нет). **Баг данных закрыт end-to-end.**

~~**🔧 РОЛЬ 2:** расширить gold-метку `ЗоЗПП` до `{18,19,20}`~~ → **✅ ПРИНЯТО Ролью 2 (2026-07-10).**

**🔧 РОЛЬ 2 — новое: перепрогнать `multicode_eval.json`, метрика должна вырасти.** Ваша цифра **54/57 = 95%** снята ДО переиндекса и содержит промах КоАП-12.9, который был **дефектом данных, а не агента**. Переиндекс уже в проде (см. выше), `status=active` → `verify` перестанет ронять статью. Ваша же проекция была **~56/57**. Это **измеримо прямо сейчас** — на защите лучше называть реальное число, а не проекцию.

**🔧 РОЛЬ 4 — цифра в шапке STATUS устарела.** В общей сводке вверху (раздел «Данные») стоит `13770 чанков / 35 актов` — после вашего же переиндекса это **13 800**. Шапка — общая зона (не правлю по карте владения). Про «35 vs 39» — обе цифры верны, единицы разные (см. врезку выше).

### 🌊 Волна 3 (2026-07-13): +18 актов — готовы к заливке

**`pipeline/acts.py`: 39 → 57 кодов (35 → 53 акта). +2009 чанков → база станет ≈15 800.** Покрытие бытовых тем ≈85% → **≈93%**. Каждый `nd` подтверждён **реальным `fetch+parse`** (не выдачей поиска — на этом обожглись с 138-ФЗ), редакции 2025-07…2026-06.

| Группа | Акты |
|---|---|
| **S** — закрывают дыры в уже работающем роутинге | микрофинансы **151-ФЗ** (keyword-net Роли 2 ловит «микрозайм», а норм не было), животные **498-ФЗ** («покусала собака»), платёжная система **161-ФЗ** (ст.9 — возврат украденного с карты), ПОД/ФТ **115-ФЗ** (блокировка счёта) |
| **A** — топ бытовых тем, не покрытых вовсе | полиция **3-ФЗ**, призыв **53-ФЗ**, больничные/декрет **255-ФЗ**, самозанятые **422-ФЗ**, кредитные истории **218-ФЗ**, инвалиды **181-ФЗ** |
| **B** — расширение | Воздушный кодекс (рейс/багаж), туризм **132-ФЗ**, нотариат **4462-I**, информация **149-ФЗ**, реклама **38-ФЗ** (спам), соцпомощь **178-ФЗ**, ветераны **5-ФЗ**, регистрация ИП **129-ФЗ** |

**Инженерные находки волны:**
- **Автопоиск `nd`**: ИПС ищет по названию параметром `a1` (не `a0` — тот полнотекстовый, 4600 результатов мусора). Выдача сортируется **по дате возрастания**, поэтому современные законы с короткими именами («О полиции») лежат на дальних страницах — нужна пагинация `start=`.
- **⚠️ Коллизии номеров** (разные законы под одним номером, развёл по дате принятия): **115-ФЗ** = ПОД/ФТ (2001) ≠ наш «о положении иностранных граждан» (2002); **218-ФЗ** = кредитные истории (2004) ≠ наш ЕГРН (2015).
- **⚠️ Старые акты — римская цифра:** нотариат это `№ 4462-I`; по «4462-1» не находится вообще.
- **✅ Старый хвост 255-ФЗ закрыт:** не выпадал поиском, потому что в ИПС лежит под **исходным** названием («Об обеспечении пособиями по временной нетрудоспособности…»), а не под нынешним.
- Фолбэк коротких законов (`savertf` → `fulltext`) сам отработал на 498-ФЗ и 422-ФЗ — прежний фикс держит.

**🔧 РОЛЬ 4 — залить 18 актов.** Коды уже в `pipeline/acts.py` (нужна пересборка образа — иначе `--act` не узнает новые коды, знакомый паттерн «стухшей сборки»). По опыту 20 актов ≈ 50–70 мин:
```bash
for a in mfo_151 animals_498 nps_161 aml_115 police_3 army_53 sick_255 npd_422 bki_218 \
         invalid_181 vk_rf tour_132 notar_4462 info_149 adv_38 socpom_178 veteran_5 reg_129; do
  python -m pipeline.run --act "$a" && sleep 20
done
```

**🔧 РОЛЬ 2 — ветки в `BRANCH_TO_ACTS` на 18 новых `act`-строк.** Без них акты **инертны**: достижимы только через «не уверен → искать по всем», точность роутинга просядет. `act`-строки — ровно как в `acts.py` (длинные имена, как показал ваш A/B). Два акта закрывают ваши же известные дыры: **микрозаймы** (keyword-net уже ловит слово) и **«покусала собака»**.

**Хвост (не блокирует).** Добрать ИПС-`nd` для **138-ФЗ** (гражданство, новый 2023 — найденный вёл на утративший силу 62-ФЗ). ~~255-ФЗ~~ — ✅ закрыт в волне 3.

## Роль 4 · Инфраструктура (`infra/`, `shared/`, корень) — ветка `Vitaliy_Svs`
- Скелет репо, `shared/contracts.py` (раздел 3 TEAM_PLAN 1-в-1), `docker-compose` (qdrant + postgres:16 + bot), миграции 3.4, `init_qdrant.py`, `Dockerfile` (tesseract-ocr-rus), `pyproject.toml` (pymupdf/pytesseract/pillow/aiohttp-socks). Фикс flat-layout сборки.
- `search_law()` (гибрид: `law_articles`/`_dev` + `user_documents` по `user_id`, `TOP_K_LAW=10`); `shared/embeddings.py` — префикс по модели (bge-m3 без префикса), `EMBED_DIM=1024`; `init_qdrant` пересоздаёт коллекцию при несовпадении размерности. `source_url` в `RetrievedChunk`/`search_law`.
- **Telegram-egress:** блок хостера (Selectel/РФ) обойдён контейнером `xray-proxy` (порты на `127.0.0.1` для всех dev-пользователей). ⚠️ Личная VPN-подписка, не командная инфра — нужно постоянное решение.
- **Докер-бот поднят и стабилен** (`STORAGE_BACKEND=postgres`, прокси, LLM-ключ в общем `.env`); реиндекс `--force` → `law_articles_dev` 623/1024 с `source_url`. И3 закрыта.
- Прод-`.env`: `LLM_API_KEY` + `LLM_BASE_URL=https://api.polza.ai/api/v1` + `LLM_MODEL` + `EMBED_MODEL=deepvk/USER-bge-m3`/`EMBED_DIM=1024` заданы. Секреты — только на сервере (не в git).
- **2026-07-08:** пересобрал образ бота под PR #21 (Роль 2). Нюанс: на момент первой попытки PR ещё не был реально смёржен, хотя в старом логе значилось «в main» (`git merge-base --is-ancestor` = `NO`) — дождался фактического мержа Ромой, тогда пересобрал (`--no-cache` по факту, не по кэшу). **Проверено живым вызовом `agent.Agent()` в контейнере**, не только по коду: `ingest_document` с настоящим PDF → `ok=True`; `answer_question` по содержимому документа ответил верно («испытательный срок три месяца»). Бот стабилен, 0 рестартов.
- **2026-07-08, боевой прогон СК РФ + ЗоЗПП:** ещё одна стухшая сборка образа (коммит `d245fc2` Роли 3 прилетел уже после последней пересборки — `--act sk_rf`/`zpp_rf` падали `invalid choice` из старого образа) — пересобрал заново, тогда прогнал `python -m pipeline.run --act sk_rf` (+183) и `--act zpp_rf` (+75). Проверил на живой коллекции: `law_articles_dev` = 881 точка (623 ТК + 183 СК РФ + 75 ЗоЗПП), payload/`source_url` корректны, ТК не тронут.
- **`search_law(query, user_id, acts=None)`** (`shared/search.py`/`contracts.py`, аддитивно) — серверный Qdrant-фильтр `MatchAny(act)` вместо клиентского пост-фильтра Роли 2 в `retrieve.py`: top-K теперь считается ВНУТРИ нужных актов, а не режется по всей базе раньше фильтра. `infra/init_qdrant.py` — payload-индекс (`keyword`) на `act` для `law_articles`/`_dev`, применил на сервере без пересоздания коллекций (данные целы). Параметр называется ровно так, как спрашивала Роль 2.
- **Сквозная проверка после пересборки:** «как взыскать алименты после развода» → ст. 84/90/91/113/24 **СК РФ**; «продавец не возвращает деньги за бракованный товар» → ст. 12/18/21/24/25 **ЗоЗПП**. Оба grounded, без отказов — живой бот отвечает по семейному и потребительскому праву. `search_law(acts=[...])` проверен отдельно (фильтрует строго, пустой список актов → 0 без падения).
- **PR #23 (Роль 2, acts-роутинг) смержен** — пересобрал образ и передеплоил ещё раз (иначе бот крутил бы старый клиентский пост-фильтр). **Проверено через полный граф `Agent()` на трёх кодексах:** отпуск→только ТК РФ (127/125/137/181.1/296), алименты→только СК РФ (90/84/113/24/91), возврат товара→только ЗоЗПП (25/24/21/18/12) — перетекания между актами нет, серверный фильтр реально работает, а не просто задеплоен впустую. Бот стабилен, 0 рестартов.
- **2026-07-08, боевой прогон Tier-2 (11 актов) + фикс редакции ЗоЗПП:** ещё одна стухшая сборка образа (коммиты Роли 3 по 11 актам + фиксу `fetch.py` прилетели после предыдущей пересборки) — пересобрал заново. Диск сервера **кончился на пересборке** (`no space left on device`, 99% занято) — 12 старых `<none>`-образов от сегодняшних пересборок никогда не чистились (~66 ГБ). Почистил (`docker container/image prune`, без `--volumes` — данные Qdrant/Postgres не тронуты), освободил 60.85 ГБ, тогда пересобрал успешно. Сначала перезалил `zpp_rf --force` (устаревшая редакция 2025-07-31 → актуальная, фикс Роли 3) — проверил на 0 сирот (пересчитал ожидаемые id чанков свежим fetch+parse+chunk, сверил с Qdrant: 75/75, орфанов нет). Затем прогнал все 11 новых актов через отсоединённый (`nohup`+`disown`) скрипт на сервере — переживает обрывы SSH (сегодня было несколько), все 11 завершились `exit=0` без ошибок, ~74 минуты суммарно (КоАП и НК ч.2 — самые долгие, по 15-20 мин каждый).
- **Итог:** `law_articles_dev` = **9229 точек / 10 кодексов** (ТК 623, УК 608, ГК ч.1-4 1846, СК 183, ЗоЗПП 75, КоАП 1502, ЖК 394, НК ч.1-2 2797, ГПК 523, УПК 678) — сумма сходится. **`search_law(acts=[...])` проверен на новых актах:** кража→строго УК РФ (158/158.1/160), наследство→строго ГК РФ (1153/1156/1145), штраф ГИБДД→строго КоАП РФ (перетекания нет; топ-3 не идеально в точку по смыслу — тот же лексический разрыв dense-поиска, что и раньше, не проблема фильтра). Бота пересобирать/передеплоивать для этого прогона не требовалось (`search_law` читает Qdrant вживую) — пересборка была нужна только чтобы у `pipeline.run` в образе появились новые `--act` коды.
- **2026-07-08, ратифицирован контракт управления документами (ответ на PR #25 Роли 2):** `shared/contracts.py` — `UserDocument`, `list_user_documents`, `delete_user_documents`, `ingest_document(filename=None)`, `answer_question(doc_ids=None)`, `RetrievedChunk.doc_id` — сигнатуры 1-в-1 с уже реализованным `agent/documents.py`/`agent/client.py`. **Моя часть — `search_law(query, user_id, acts=None, doc_ids=None)`:** серверный Qdrant-фильтр по `doc_id` для `user_documents` (`MatchAny`, тот же паттерн, что `acts` для `law_articles`), аддитивно. `infra/init_qdrant.py`: payload-индекс `user_id`(int)/`doc_id`(keyword) на `user_documents`, применил на сервере без пересоздания коллекции.
- **Попутно найден и исправлен баг упаковки (тянулся с 2026-07-07):** `pyproject.toml` `[tool.setuptools] packages=["bot","agent","shared","pipeline"]` ставил ТОЛЬКО верхний уровень — подпакеты `agent/llm`, `agent/nodes`, `agent/tools`, `bot/handlers` в `pip install .` не попадали. Маскировалось случайно: `python -m bot.main` добавляет `/app` (сырые исходники) в `sys.path`, перекрывая неполную установку сайт-пакетами; всплыло, когда прогнал голый `pytest` вне `/app` (мимо этой маскировки) — `ModuleNotFoundError: agent.llm` при сборе `tests/test_agent_graph.py`. Переключил на `[tool.setuptools.packages.find]` с `include=["bot*","agent*","shared*","pipeline*"]`/`exclude=["bot.tests","tests"]` — подпакеты подхватываются автоматически (и будущие тоже), тесты не попадают в инсталляцию. **Проверено на сервере:** чистый `pip install .` + голый `pytest` → **все 71 тест зелёные** (`tests/`+`bot/tests`), впервые без опоры на случайную маскировку sys.path.
- **Живой e2e документов на пересобранном образе:** 2 PDF с разными именами → `list_user_documents` (оба, с именами/датами) → `search_law(doc_ids=[...])` нашёл контент строго нужного документа (не перепутал оклады двух договоров) → `delete_user_documents(doc_id=X)` удалил один, второй остался → `delete_user_documents()` без `doc_id` вычистил всё. Тестовые данные из прод-базы убраны. Бот стабилен, 0 рестартов.
- **2026-07-08, пересборка под PR #26/#28/#29 (Роль 1 — бот-часть документов).** Образ отставал: собран в 16:00 MSK, а `/documents`+delete-purge (16:15), скоуп-пикер (16:36) и агрегация альбома (16:49) прилетели позже — пересобрал, подтвердил маркеры в site-packages (`content.py`/`commands.py` с `media_group_id`) до деплоя. Живой контейнер стабилен, 0 рестартов. **Вся цепочка документов (список/скоуп/удаление/152-ФЗ/агрегация альбома) теперь реально в проде, а не только в `main`.**
- **2026-07-08, пересборка под PR #30/#31/#32 (Роль 2 — квота/fast-path, HyDE, confidence-телеметрия).** Тот же паттерн: образ собран в 17:20 MSK, а квота+fast-path (17:26), HyDE-фьюжн (18:30) и confidence-телеметрия (18:40) прилетели позже — пересобрал заново. Маркеры в site-packages подтверждены до деплоя (`_search_with_quota`/`retrieve.py`, `HYDE_ENABLED`/`config.py`, `log_confidence`/`confidence_log.py`). Диск/место не проблема (52 ГБ занято из 99 ГБ). Задеплоил, 0 рестартов, лог штатный (`Хранилище: postgres`).
  - **Живая проверка через реальный `Agent()` (не только код):** «статья 158 УК РФ» → fast-path, ст. 158 первой строкой в цитатах; «как оспорить решение суда первой инстанции» (житейская формулировка) → ст. 320 ГПК первой строкой — совпадает с заявленным Ролью 2 результатом. `confidence_log`: 444→446 строк после двух вызовов (телеметрия пишется на живом Postgres, `POSTGRES_DSN` из общего `.env` подхватился, отключения на ошибку не было).
  - `AGENT_HYDE` в `.env` не трогал — дефолт `"1"` (включён) в `agent/config.py`, отдельная переменная не требуется. **На заметку:** HyDE = +1 параллельный вызов `gemini-2.5-flash-lite` на каждый вопрос — если лимит Polza станет проблемой, это первый кандидат на `AGENT_HYDE=0` без пересборки образа (только рестарт контейнера).
- **2026-07-09, боевой прогон +5 актов Роли 3 (ЗК/КАС/229-ФЗ/127-ФЗ/ОСАГО, PR #33 роутинга уже смёржен):** тот же паттерн стухшей сборки — образ не знал новых `--act` кодов (`invalid choice`), пересобрал `--no-cache` (заодно нашёл, что хостовый `.venv` вообще без `sentence_transformers`/`torch` — реальный прогон `pipeline.run` идёт только через `docker exec` в контейнер бота, где зависимости есть; на будущее — не пытаться гонять пайплайн напрямую из хостового venv). Подтвердил маркеры (5 новых кодов в `--help`) до деплоя, задеплоил, 0 рестартов. Прогнал все 5 через `nohup`+`disown` внутри контейнера — пережил обрыв SSH (сеть до сервера сегодня шумела), все exit=0, ~25 минут суммарно.
  - **Итог:** `law_articles_dev` = **11489 точек / 15 актов** (+2260: ЗК РФ 382, КАС РФ 503, 229-ФЗ 236, 127-ФЗ 1033, ОСАГО 106) — сумма сходится, старые 12 актов не тронуты. **`search_law(acts=[...])` проверен на всех 5 новых:** раздел земельного участка→строго ЗК РФ (ст.11.4/11.5), обжалование решения в суде→строго КАС РФ (ст.202/245/249), арест имущества приставами→строго 229-ФЗ (ст.80/95), банкротство физлица→строго 127-ФЗ (ст.213.4/213.5/223.2 — глава X про потребительское банкротство, в точку), выплата по ОСАГО→строго ОСАГО (ст.17/12). Перетекания между актами нет.
- **2026-07-09, боевой прогон +20 бытовых ФЗ Роли 3 (кредиты…приватизация, PR #35/#37 роутинга уже смёржены) + force-переиндекс 2 устаревших актов + перепроверка `koap_rf`:** тот же паттерн стухшей сборки — пересобрал образ `--no-cache` (20 новых `--act` кодов + харденинг Роли 2 из PR #36/#37), подтвердил все 20 кодов в `--help` до деплоя, задеплоил, 0 рестартов. Прогнал 20 актов через `nohup`+`disown` на сервере (пауза 20с между актами, ИПС троттлит) — пережил очередные обрывы SSH-мониторинга (сама фоновая задача на сервере не прерывалась), все 20 завершились без ошибок в логе, ~50 минут суммарно. Затем **force-переиндекс** `gk_rf_4` (398 точек, подтянул ред. 2025-07-23) и `gpk_rf` (523 точки, ред. 2026-03-31) — оба чисто, орфанов нет (пересчитал точный total по коллекции до/после — сошлось). **Перепроверил `koap_rf`** обычным прогоном (без `--force`, версионирование по хэшу само решает, что обновлять) — снял пробел из аудита Роли 3 (там ИПС рвал соединение), подтверждена редакция 2026-05-02, 1502 чанка пересчитаны и записаны.
  - **Итог:** `law_articles_dev` = **13770 точек / 35 актов** (+2281 новых чанков от 20 актов: потребкредит 86, ипотека 149, ДДУ 130, перс.данные 54, ЕГРН 349, СНТ 91, ЗАГС 113, пенсии 105, здоровье 199, ОМС 127, прописка 19, образование 328, миграция 210, обращения 20, коллекторы 51, маткапитал 38, опека 40, занятость 105, пособия на детей 45, приватизация 22 — сумма проверена точным подсчётом по Qdrant, не по логам). ГК/ГПК/КоАП обновлены контентно, без изменения числа актов. **`search_law(acts=[...])` проверен на 7 актах (5 новых бытовых + 2 обновлённых кодекса):** потребкредит, маткапитал, ЗАГС, коллекторы, приватизация — строго свой акт; наследство→строго ГК РФ, парковка→строго КоАП РФ (оба после переиндекса) — 7/7, перетеканий нет. Бот стабилен весь прогон, 0 рестартов.
- **2026-07-10, три пересборки образа за день (Роль 2):** (1) фикс утечки маркера `INSUFFICIENT` (`_is_insufficient` — регэксп по границе слова вместо `startswith`); (2) `COMPOSE_DOC_SYSTEM` п.5 — прямой ответ на вопрос о мошенничестве (вероятность, не обвинение); (3) выравнивание цитат «Основание↔текст» (`_align_citations` в `compose.py`). Каждый раз маркер фичи (`_is_insufficient`, обновлённый текст промпта, `_align_citations`) проверен прямо в собранном образе (`docker run --rm ... python -c "..."`) ДО деплоя — паттерн стухшей сборки за проект встречался достаточно, чтобы не пропускать эту проверку. Все три задеплоены по очереди, каждый раз 0 рестартов, логи штатные.
- **2026-07-10, полный переиндекс `--force` всех 39 актов (задача Роли 3 — фикс ложного `repealed`, когда «часть утратила силу» ошибочно гасила всю статью в `parse.py`):** прогнал командой Роли 3 через `nohup`+`disown` внутри контейнера бота — пережил несколько обрывов SSH за ~4 часа прогона (08:38–12:38 MSK), сама фоновая задача на сервере не прерывалась. **39/39 `exit=0`.** Единственная заминка — `502 Bad Gateway` от ИПС на `ip_229` (лог показал `FetchError` на первой попытке), самовосстановился на встроенном retry пайплайна, финальный count совпал с ожидаемым (236 чанков, орфанов нет). Точечно проверил: КоАП 12.9 (скорость) — раньше ложно `repealed`, теперь `status=active`. **Итог:** `law_articles_dev` = **13800 точек / 39 актов** (было 13770 — небольшой рост, естественный: переиндекс подтягивает самые свежие редакции при повторном fetch). Бота пересобирать/передеплоивать не потребовалось — правится только тег `payload.status`, `search_law` по нему не фильтрует, контент/эмбеддинги не менялись, живой трафик не прерывался.
- **2026-07-13, пересборка под PR #49 (Роль 1 — фикс потери контекста при уточнении).** Образ отставал: последняя сборка была от 2026-07-10 08:35:57 UTC, а PR #49 (`pending_question`/`_with_clarification_context` в `bot/handlers/content.py`) смержен только сегодня утром — проверил коммиты между сборкой и HEAD построчно: оба промежуточных коммита за 10-е трогали только `STATUS.md`, реального отставания не было вплоть до сегодняшнего PR #49. Диск почистил перед сборкой (`docker image prune`, без volumes — 33 ГБ освобождено, данные не тронуты), пересобрал `--no-cache`. **Маркеры фикса** (`pending_question`, `_with_clarification_context`, `awaiting_clarification`) подтверждены прямо в собранном образе ДО деплоя. Задеплоил, 0 рестартов, лог штатный (`Хранилище: postgres`, прокси, polling). **Тест-сьют `bot/tests/` внутри контейнера (pytest поставил временно, в образе его нет по умолчанию): 61/61 passed**, включая оба новых `test_clarify.py` (`test_clarification_reply_keeps_original_question`, `test_normal_question_not_wrapped`). Прямой e2e через реальный `Agent()` не показателен для этого бага (баг чисто в FSM-обёртке бота, не в агенте; тестовый вопрос сразу получил ответ без уточнения) — тест-сьют команды это покрывает лучше. SSH дважды обрывался во время проверки (сеть шумела, как отмечено раньше в журнале) — контейнер и фоновые операции не прерывались.
- **⚠️ Пока разворачивал PR #49, в `main` прилетел ещё один мерж (Роль 2, `a721164`, «OCR-гейт + предобработка фото»), трогает `agent/ingest.py`/`agent/tools/parse_pdf.py` — в этом образе его ЕЩЁ НЕТ, нужна отдельная пересборка.**
- **2026-07-13, пересборка под OCR-гейт + Citation.text (Роль 2, коммиты eb094c3/a721164, 6316754/fa4044b).** Диск в этот раз в порядке (59 ГБ своб.), пересобрал `--no-cache` сразу с новым HEAD. **Маркеры до деплоя** (в собранном образе, `docker run --rm`): `_has_meaningful_text`/`OCR_MIN_WORDS=4` (`agent/ingest.py`), `_preprocess`/`OCR_MIN_SIDE=1600` (`agent/tools/parse_pdf.py`), `Citation.text` в `shared/contracts.py` + `CITATION_TEXT_CHARS=450` (`agent/config.py`) — все на месте. Задеплоил, 0 рестартов, лог штатный. `bot/tests/` — 61/61 (агентский `tests/` физически не попадает в образ, как и раньше — известное свойство пакетирования, не регрессия). **Живая функциональная проверка через реальный `Agent()`** (агентский pytest-сьют недоступен в контейнере, поэтому вместо него): вопрос «можно ли уволить в отпуске» → цитаты ТК РФ ст.127/137/125, у каждой **`Citation.text` реально заполнен** дословной выдержкой статьи; `_has_meaningful_text` на мусорной OCR-строке → `False`, на реальном тексте → `True`; `_preprocess` апскейлит мелкое фото 100×50 → 1600×800. **LLM-as-Judge** (`agent/eval/*`, коммиты e0b6d43/ecbea04) заехал в образ попутно, но живого прод-пути не касается (grep подтвердил: нигде не импортируется из `agent/graph.py`/`agent/nodes/`/`bot/`) — офлайн-инструмент команды, деплоя не требовал сам по себе. **Итог: образ полностью соответствует HEAD `main` (`a8f0292`), отставаний не осталось.**
