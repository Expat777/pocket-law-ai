# STATUS — живой журнал команды

> Единственный источник правды. Тезисно: состояние, возможности, открытые задачи, операционные знания.
> Сжат 2026-07-08 (551 строка → лог) и повторно 2026-07-13 (298 → ~110): хронология PR-за-PR убрана — она в git-логе.

## Правила
1. **Начало сессии:** `git pull origin main`, дать Claude прочитать этот файл.
2. **Конец блока:** правишь **только свою** секцию. Коммит `status: роль N` — сразу в main без PR. Пуш отбило → `git pull --rebase origin main`.

---

## 🟢 Состояние системы (MVP в проде)
- **Бот живой:** @pocket_law_ai_bot — Selectel VPS (Ubuntu 24.04), docker-compose (bot + qdrant + postgres + xray-egress), long polling, `STORAGE_BACKEND=postgres`. Образ соответствует HEAD `main`.
- **Данные:** `law_articles_dev` = **15 809 точек / 53 акта (57 кодов)** — 12 кодексов + 41 закон (реестр: `pipeline/acts.py`), эмбеддинги `deepvk/USER-bge-m3` (1024), `source_url` в payload → кликабельные цитаты. Покрытие ≈93% бытовых юр-тем.
- **LLM:** Polza.ai `google/gemini-2.5-flash-lite` (смена = `LLM_MODEL`), ретраи на 503; Whisper STT на том же ключе/базе.
- **Метрики к защите:** мультикодекс Recall@цитаты **≈93% (53/57, медиана 3 прогонов)** + doc-консультация 3/3; ТК сквозной **98% (61/62)**; routing purity **100%**; anti-hallucination подтверждён (цитаты только из verify, негрунтованное не попадает).
- **Функционал:** ответы с цитатами + дословные выдержки статей; документы PDF/фото/URL (мультискоуп, авто-скоуп, `/documents`, `/delete` чистит и Qdrant — 152-ФЗ); голосовой ввод с эхом распознанного; PDF-памятка с источниками; `/topics`; уточнение без потери контекста.

## 🔴 Открытые задачи
| Кому | Что |
|---|---|
| ~~Роль 2~~ | ~~**«Покусала собака»**~~ **✅ закрыто 07-13:** корень — не квота: intent называл только «гражданское», и серверный фильтр `acts` отрезал 498-ФЗ ДО ранжирования (сырой ретрив был без фильтра, потому и видел его 2-м). Фикс — keyword-net `собак/выгул` → 498-ФЗ. Live 4/4: «покусала» → ГК 1064 + 498-ФЗ ст.21/9, «выгул без намордника» → ст.13 первой (см. секцию Роли 2) |
| ~~Роль 2~~ | ~~**Температура по узлам**~~ **✅ сделано 07-13, гипотеза проверена гейтом:** intent/HyDE → 0.0 (per-call оверрайд в `LLMClient.complete`, ручки `AGENT_INTENT/HYDE_TEMPERATURE`), compose не тронут. Гейт (стабильность роутинга, 12 вопросов × 3 прогона): попадание акта **36/36 в ОБОИХ режимах** — разброс 51/55/53 НЕ от intent, он в композиции; детерминизм подрос (acts 9→10/12, идентичные retrieval_query 0→4/12; полного детерминизма у Gemini нет и при 0.0). Оставлено как гигиена (см. секцию Роли 2) |
| Роль 2 | Отложено по согласованию: recall МРОТ ст.133/136 в doc-аудите; качество прозы «номер-к-нарушению» |
| ~~Роль 4~~ | ~~**Автообновление НПА — шаги А→Б→В**~~ **✅ задеплоено 07-14:** volume `pipeline_state` в docker-compose (без пересборки, 0 рестартов) → `--seed` 57/57 актов получили базовую линию → cron хоста `0 4 * * *` (`docker compose exec -T bot python -m pipeline.watch`, лог `~/pocket-law-ai/law_watch.log` — `/var/log` под `dev4` не пишется) |
| Роль 4 | Branch protection на `main`; постоянное egress-решение вместо личного VPN |
| ~~Роль 4~~ | ~~**`.env.example` отстал от кода на 11 ручек**~~ **✅ 07-14:** добавил все 11 (LLM_TEMPERATURE, AGENT_HYDE, AGENT_INTENT/HYDE_TEMPERATURE, AGENT_ANCHOR_ARTICLES, STT_MODEL/LANGUAGE/MAX_BYTES + STT_TIMEOUT, AGENT_CITATION_TEXT_CHARS, AGENT_DOC_CONTEXT_CHARS, JUDGE_MODEL) секциями «настройки агента / STT / eval»; исправил устаревший комментарий «LLM API (только генерация)» → «intent+compose+HyDE+STT»; помечено «все опциональны, полный список в agent/config.py» |
| ~~Роль 4~~ | ~~**`Agent.transcribe_voice` не ратифицирован**~~ **✅ смержено 07-14 (PR #53):** сигнатура `transcribe_voice(user_id, audio_bytes, filename=None)->str` в разделе 3.1 `contracts.py` (1-в-1 с `agent/client.py` и стабом бота). Контракт ратифицирован ×4, реализация больше не опережает контракт |
| ~~Роль 4~~ | ~~**`make fixtures` бит**~~ **✅ 07-14:** цель удалена из Makefile (каталога `agent/fixtures/` давно нет); в quick-start (README/CLAUDE.md/ADVOCAT) заменил `make fixtures` на актуальное наполнение через пайплайн `docker compose exec -T bot python -m pipeline.run --act tk_rf` |
| Владелец ключа | Лимит трат на аккаунте Polza |
| Фаза 2 | Тезаурус/граф статей; `law_versions` по-статейно; полноценный «режим аудита договора»; идеи Роли 1 (проверка документа на противоречия закону, вывод confidence юзеру — требуют строки в контракте `Answer`) |

---

## Роль 1 · Bot (`bot/`) — ветка `Dordzhi-Msk` · **DoD закрыт, всё в проде** (73 теста)
- aiogram 3.x, long polling. Команды: `/start` (обязательное согласие 152-ФЗ), `/help`, `/documents`, `/delete`, `/topics`. FSM, 3 ветки контракта. Форматтер MarkdownV2: экранирование, «Основание: ст. N» + дословная выдержка курсивом (`Citation.text`), разбивка >4096, кликабельные `source_url`, мягкая деградация. `PostgresRepository`: согласие/история/rate-limit переживают рестарт.
- **Документы (UX end-to-end):** приём PDF/фото (magic-bytes, ≤20МБ) и URL; альбомы агрегируются по `media_group_id`; гейт «⏳ обрабатываю документ» (ref-count против гонок); `/documents` — мультивыбор скоупа (✓ на нескольких, «Отметить все»/«Сбросить»), липкий per-user; **авто-скоуп на свежезагруженный документ**; вопрос в подписи к файлу/ссылке отвечается сразу по нему.
- **Голос:** `on_voice` → скачать OGG → `Agent.transcribe_voice` → эхо «🎙 Вы спросили: …» (контроль распознанного — обязателен, Whisper галлюцинирует на не-речи) → обычный поток. Лимит 300с, мягкие деградации.
- **Экспорт:** кнопка «📄 Скачать с источниками» → PDF-памятка (PyMuPDF, шрифт helv, кириллица 1:1, оформлено как справка с дисклеймером); защита от повторных тапов.
- **Уточнения:** при переспросе исходный вопрос сохраняется в FSM (`pending_question`) и склеивается с ответом пользователя — контекст не теряется.
- Наблюдение (не баг): rate-limit обнуляется через `/delete`+`/start` — законная реализация права на удаление.
- **Регресс-артефакт:** эталонный PDF «трудовой договор с 8 нарушениями ТК» (юр-тест 07-10) — воспроизводимый кейс для проверки doc-режима, держит Роль 1.
- **Презентация консолидирована → `defense-presentation.html` (07-17):** финальная защитная дека — единый self-contained файл `defense-presentation.html` в корне (правят все роли по своим зонам, онлайн-копия артефактом claude.ai — держит Роль 1). Ранние черновые standalone-деки `presentation.html` и `presentation-alt.html` удалены из репо как устаревшие. Записи ниже от 07-14 про `presentation.html` — исторические.
- **Презентация проекта (историческое, 07-14) — общий ресурс в репо:** `presentation.html` (корень, self-contained standalone — открывается в браузере двойным кликом, без зависимостей). **Любая роль правит сама** (Роль 2 уже добавила слайды по своей зоне). Отрисованная схема всей системы, светлая пергамент-тема. Слайд «Куда дальше / позиционирование» (материал Роли 2) — сверстал и добавил. Онлайн-копия (артефакт claude.ai) синхронизирована из репо — держит Роль 1.

## Роль 2 · Agent (`agent/`) — ветка `Roman_SPT` · **функционально завершена**
- Граф `intent → retrieve → verify → {compose|clarify|refuse|offtopic}`, `Agent()` drop-in; LLM абстрагирован; бюджеты (max_tokens 900, recursion 8, обрезка 4000). **Anti-hallucination:** цитаты только из verify; `INSUFFICIENT` ловится по границе слова где угодно; промпт-инъекции (файл/URL/социнженерия) проверены на живом LLM.
- **Ретрив-стек:** роутинг **53 ветки** (`BRANCH_TO_ACTS` — единый источник правды, промпт автогенерится, канарейка синхронности); квота top-K на акт + **round-robin** по актам; **keyword-net** (ипотека/осаго/микрозайм/МФО/маткапитал/коллекторы; омоним «коллектор»-сантехника разведён); номерной **fast-path** («ст.158 УК»); **HyDE-фьюжн** (`AGENT_HYDE`, параллельно intent, сбой не роняет ответ); **якоря фундамент-статей** (залив/собака→ГК 1064, ДТП→ГК 1079; каждый пункт валидирован судьёй; `AGENT_ANCHOR_ARTICLES`).
- **Цитаты:** выравнивание «Основание» = {статьи в прозе} ∩ {найдено ретривом} — мусор и негрунтованные номера не попадают; `Citation.text` — дословная выдержка ≤450 симв из ретрив-чанка (grounding, не LLM).
- **Документы:** ingest PDF+OCR(rus) с **гейтом качества** (<4 словоподобных токенов → «пришлите чётче», стоп галлюцинациям на фото) и предобработкой (EXIF/грейскейл/автоконтраст/апскейл 1600px); URL с SSRF-защитой; изоляция по `user_id`; **doc-режим: документ ведёт разбор** (`COMPOSE_DOC_SYSTEM`: что это / что хотят / по закону / что делать / антифрод как ВЕРОЯТНОСТЬ, прямой ответ на «это мошенники?»); разбор идёт по упорядоченной голове документа, не по top-K.
- **STT:** `transcribe_audio` (Whisper через Polza, тот же ключ; ручки `STT_MODEL/STT_LANGUAGE/STT_MAX_BYTES`).
- **LLM-as-Judge** (`agent/eval/`): офлайн-судья (temp=0, рубрика grounded/correct/citations/complete, 0-8), валидирован на эталонах; **отсеял 3 «улучшения»** (чеклист договора — утечка эталона в первом замере; RR-лимит — регресс на обеих группах; глобальное снижение температуры — не помогает) — метрическая дисциплина против иллюзий.
- **Известное:** стабильный промах ТК-81 («уволить в отпуске» — ст.81 не ретривится, семантический разрыв; якорь проверен судьёй и отклонён — модель не называет ст.81 в прозе, расширять метку = подгонка); остальные промахи скачут между прогонами (вариативность композиции ±2); confidence-телеметрия пишется в Postgres на всех юр-исходах.
- **Полный аудит зоны (07-13): 8 находок, все закрыты** (31/31 тестов + live-смоук). Главные: «Основание» теряло ПЕРЕЧИСЛЕНИЯ статей («ст. 115, 133 ТК» ронял 133, «статьями 91 и 92» не матчился) → регэксп на списки/словоформы в compose И fast-path (live: «сравни ст. 91 и 92 ТК» → обе цитаты); `verify_citation` считал живой только `active`, а контракт допускает `amended` (изменена, но В СИЛЕ — латентная тихая потеря норм класса КоАП-12.9 при фазе 2 Роли 3) → active|amended|None; кэш Qdrant-клиентов в lookup/ingest (был новый клиент на каждый вопрос с якорем); `graph.ingest_document` терял `filename` из контракта 3.1; STT-ретраи на 503 (как у LLM); `content:null` → "".
- **«Покусала собака» (репро Роли 4) — закрыто:** keyword-net `собак/выгул` → 498-ФЗ (intent на 53 ветках терял вторичную ветку, фильтр acts отрезал закон до ранжирования — паттерн «ипотеки» из бага A). Live 4/4: покус → ГК 1064 (якорь) + 498-ФЗ ст.21/9; выгул → ст.13 первой.
- **Реальный eval с Pravoved (идея пользователя, 07-13):** собрал 40 живых клиентских вопросов / 18 рубрик (robots-дружелюбно, ПДн-скраб, в репо не кладём; 9111/pravorub не подошли). Прогон агент+судья: **средний 4.4/8, медиана 4** — против ~7/8 на рукописном eval: реальная речь заметно тяжелее. Хорошее: честные отказы на непокрытых темах работают (7 REF: таможня/расчёты/военные выплаты), «книжные» кейсы держатся и в живой речи (4×8/8: завещание, парковка-инвалиды, недееспособность, авто в браке). Три корня слабости: **(а)** на реальной прозе модель редко пишет «ст. N» → выравнивание цитат падает в fallback top-5 ретрива → главная жалоба судьи «цитаты не соответствуют ответу»; **(б)** непокрытая тема (военное право) + семантические соседи из базы = уверенный МИМО-ответ 0/8 (опаснее честного отказа); **(в)** мультитемные длинные вопросы тянут мусорные акты в цитаты через квоту/RR (498-ФЗ на вопросе про вывоз ТКО — семантика, не keyword-net). Корпус переиспользуемый — готовая площадка для A/B следующих фиксов.
- **Фиксы по реальному eval — ГЕЙТ ПРОЙДЕН, в main (07-13): 4.39 → 6.12/8 (медиана 4→7, «восьмёрок» 4→14).** Три изменения, A/B на том же корпусе: **(а)** COMPOSE_SYSTEM обязывает сопровождать каждое утверждение «(ст. N АКТ)» (только реально используемые из блока СТАТЬИ) → выравнивание цитат заработало на реальной речи, жалобы «цитаты не соответствуют» исчезли, мусор из «Основания» ушёл (ТКО 5→8/8, «животные» пропали); **бонус — закрылся флагманский промах ТК-81** («уволить в отпуске»: модель называет ч.6 ст.81 в прозе → статья поднимается в цитаты; якорь это не брал); **(б-1)** военная тема → 53-ФЗ (ветка «военная служба» + keyword-net `сво/мобилизац/военнослужащ`): «вернуть мужа с СВО» 0/8 → 5/8 с цитатой 53-ФЗ ст.51; **(б-2)** uncovered-гейт: тема целиком вне базы (`UNCOVERED_BRANCHES`, пока таможня) → честный отказ «тема не покрыта» вместо уверенного мимо-ответа по семантическим соседям. Регрессия рукописного набора: сабсемпл 14/15 = 93% (число к защите держится). 33/33 тестов. Роль 4: подхватится пересборкой.
- **Guard «правила площадок не толкуем» — прогнан гейтом, ОТКЛОНЁН (07-14):** пробовал промпт-запрет толковать внутренние правила сервисов (Ozon/WB/Telegram «п. N их правил»). A/B (3 platform + 4 legit + контроль): legit-потребительские без ложных отказов, но на platform пользы нет (WB и так refused; Озон обе версии 7/8 с корректными общими нормами), а на Телеграм-кейсе guard спровоцировал РЕГРЕСС (честный отказ → 4/8 с мусорным КоАП 30.1) → не мержим (4-е отклонённое судьёй «улучшение»). Проверка Озон ×3 на текущем main: 4/4/5 — опасная фабрикация ушла ещё с фиксом (а) (цитаты грунтованные потребительские, вчерашний 0/8 с ГК-интеллектуалкой не воспроизводится); остаток — модель спекулирует содержанием «п. 2.3» (общие нормы + догадка). **Известное ограничение задокументировано:** ультра-краткие вопросы о правилах площадок = 4-5/8, лечится только уточнением у пользователя, отдельная механика не окупается.
- **Квантизация / прунинг / дистилляция — рассмотрены, НЕ подходят проекту (07-14):** разбор по нашей архитектуре. (1) LLM хостовая (Polza, `gemini-flash-lite`) — веса не наши, GPU нет → техники неприменимы в принципе, смена модели = `LLM_MODEL`, а не квантизация. (2) Векторная квантизация в Qdrant бессмысленна: индекс ~65 МБ (15 809×1024 float32) — это инструмент для миллионов точек, у нас только добавит риск recall за ноль выгоды. (3) Эмбеддер bge-m3 (единственное, что крутим сами, на CPU) — не бутылочное горло (латентность съедают 2-3 вызова LLM по ~5с, не один прогон эмбеддера) и load-bearing для recall (Роль 3 бенчмарком: reranker/гибрид роняют качество); квантизация потребовала бы переэмбеда всего корпуса (рассинхрон вектора запроса и корпуса) — кросс-ролевая операция Ролей 3/4. **Главное — направление:** все три техники уменьшают модель ЦЕНОЙ качества, а наше узкое место — КАЧЕСТВО, не стоимость/латентность (мы quality-bound; рычаг = более СИЛЬНАЯ модель, т.е. анти-квантизация). Осознанно отклонено — обоснованный негативный результат для защиты, как reranker у Роли 3. intent/HyDE = 0.0 (поисковая половина, детерминизм), compose остался на `LLM_TEMPERATURE` 0.2 — через per-call оверрайд `complete(..., temperature=)`; ручки `AGENT_INTENT_TEMPERATURE`/`AGENT_HYDE_TEMPERATURE`. **Гейт — стабильность роутинга, не судья:** попадание акта 36/36 в обоих режимах (разброс метрики 51/55/53 — НЕ intent, а композиция), стабильность actов 9→10/12, идентичность retrieval_query 0→4/12. Честный итог: гигиена + стабильнее трейсы, не лекарство от разброса. 32/32 тестов.
- **STT-квантизация — уточнение к пункту выше по итогам командного обсуждения (07-14, ИЗМЕРЕНО):** в разборе по агенту отдельно выделили STT как особый случай. Итог: из трёх техник к нам применима ТОЛЬКО квантизация и ТОЛЬКО к Whisper, если заносить STT in-house (прунинг/дистилляция требуют дообучения на своих данных — ни одну модель мы не тренируем; готовый distil-whisper слаб на русском → это выбор модели, не наша дистилляция). Замер `faster-whisper small int8 CPU`: латентность отличная — RTF **0.15** (сервер Xeon Gold, TTS) и **0.21** (реальный голос: 10 живых голосовух пользователя, 144с аудио→30.5с инференса, ~3с/клип), модель тёплая после разовой загрузки. Качество: на чистом TTS юр-лексика дословно (номера статей, «защите прав потребителей», «неустойку»); на РЕАЛЬНОЙ разговорной речи суть восстановима во всех 10, но роняет отдельные термины («алименты»→«элементы», «микрозаймов»→«микрозамых», «кредит»→«кривит», «уволить»→«вольт») — это **валидирует эхо-подтверждение расшифровки (Роль 1) как обязательную механику**, оно данный риск и закрывает. **Вывод:** in-house квантованный Whisper ЖИЗНЕСПОСОБЕН; развилка не в скорости, а качество-vs-приватность — hosted large (макс. точность, но голос-ПДн уходит в Polza) против in-house small/medium int8 (голос остаётся на нашем сервере, опора на эхо-подтверждение). НЕ «отклонено» — измерено и отложено до триггера (голос как узкое место по цене/латентности ИЛИ приоритет резидентности голос-ПДн по 152-ФЗ). **Сравнение small vs medium int8** (те же 10 записей): medium чинит бОльшую часть потерянных термов (кредит/уволить/микрозайм/«долги банкам»), но ×3 латентности (RTF 0.21→0.68, ~8-9с/клип на Ryzen) + редкая петля-повтор; самые трудные (алименты→«элементы», смазанная разговорная речь) не берёт и он. **Рекомендация — сейчас НЕ внедрять:** приватностный выигрыш ЧАСТИЧНЫЙ (текст вопроса и так уходит в Polza для генерации ответа — in-house оставляет локально лишь СЫРОЙ звук/голосовой отпечаток), качество проседает vs hosted large, а STT — не наше узкое место (латентность съедают вызовы LLM). Держим измеренной опцией под триггер: рост голосового трафика ИЛИ ужесточение требований к резидентности голос-ПДн. Голос обрабатывался локально (ПДн не выгружались), тест-окружение за собой почищено.
- **Верификация деплоя 07-14 + флаг рассинхрона серверных чекаутов (→ Роль 4):** после пересборки Роли 4 (`78e5255`) проверил ЖИВОЙ образ — `2026-07-14T07:23:48Z`, все **7 маркеров Роли 2 в `/app`** (`UNCOVERED_BRANCHES`, `AGENT_INTENT_TEMPERATURE`, `военная служба`, `собак|выгул`, `_ARTICLE_NUM`, `«115 ТК РФ»`, `amended`); `pipeline/watch.py` в образе = main. **Наш код live ✅**, стухшая сборка вылечена. **НО серверные гит-чекауты рассинхронены:** `/home/dev2/pocket-law-ai` торчит на ветке `Roman_SPT`@`251d886` (стухшая дев-ветка), а его `origin/main` = `c8b4e04` (даже `git fetch` не делали — реальный HEAD `78e5255`). Стек поднят из `/home/dev4/pocket-law-ai` (сверено по compose-лейблам), volume `pipeline_state` смонтирован — поэтому ПРОД не задет, но это мина класса «стухшая сборка» для будущих ребилдов/cron. **→ Роль 4:** привести server-чекауты к `main` + `git fetch/pull`; убедиться, что деплой-каталог `dev4` на актуальном `main` ПЕРЕД следующей пересборкой; чекаут `dev2` на моей ветке — вычистить/переключить.

- **Презентация: +2 слайда и правка диаграммы (07-14, по согласованию с командой — правим общий `presentation.html` Роли 1):** добавил слайды «Скоуп ввода» (текст/PDF/скан/фото/голос со стороны Агента, с анти-выдумкой на каждом уровне) и «Как измеряем качество» (Pravoved 4.39→6.12, LLM-судья, 0 плацебо, механизм грунтовки). На runtime-диаграмме назвал движки: OCR — **Tesseract** (локально), STT — **Whisper**; развёл «PDF→текст-слой» и «скан/фото→OCR (Tesseract)». Дека 12→14 слайдов, счётчик/точки-навигация динамические (JS по slides.length). Превью-артефакт согласован.

- **→ Роль 1: слайд «Куда дальше / позиционирование на рынке» (материал готов — доделать и вставить).** Обсудили с командой рыночную зрелость агента; для защиты полезен честный слайд «куда двигаться дальше». Готовые тезисы для слайда:
  - **Дифференциатор:** анти-выдумка (грунтованные цитаты + честный отказ) обгоняет наивных «AI-юристов» — обёрток над ChatGPT, которые галлюцинируют статьи. Это наш главный рыночный аргумент.
  - **Ниша:** бесплатный мгновенный цитируемый ответ по бытовым вопросам граждан (НЕ конкурент КонсультантПлюс/Гаранту для профи и НЕ маркетплейсам живых юристов — другой сегмент).
  - **До прод-выпуска нужно:** юр-валидация на сотнях кейсов живыми юристами; дисклеймер «информация, не юр-консультация» + эскалация к юристу на пограничных вопросах; расширение покрытия (сейчас 53 акта, нет судебной практики / подзаконки / разрешения конкуренции норм); модель сильнее (сейчас quality-bound на flash-lite).
  - **Путь:** сузить до 2-3 сильных вертикалей (ЗоЗПП, трудовое — там ~7-8/8) → дисклеймеры/эскалация → валидация юристами → бета с реальными пользователями.
  - **Тезис слайда:** «доверенное ядро (анти-выдумка + измеримость) готово и обгоняет наивных ботов; до полного продукта — юр-валидация на масштабе, покрытие практики и рамка ответственности; путь понятен».
  - Оформление/вёрстка — за Ролью 1 (стиль деки); Роль 2 даёт содержание.

## Роль 3 · Pipeline (`pipeline/`) — ветка `Roma_MSK` · **DoD закрыт** (42 теста)

> **Цифры к защите (57 и 53 — обе верны, единицы разные):** **57** = кодов пайплайна (`--act`) · **53** = уникальных `act` в payload — многочастные кодексы под одним act (ГК ч.1–4 → «ГК РФ», НК ч.1–2 → «НК РФ»). Слайд: **«53 акта (12 кодексов + 41 закон), 57 источников, 15 809 чанков»**.

- **Пайплайн:** `fetch → parse → chunk → embed → upsert`, CLI `python -m pipeline.run --act <code>` (`--force` — перезалить игнорируя state; `--dry-run`). Источник — ИПС pravo.gov.ru, экспорт последней действующей редакции. Чанк = **статья** (длинные режутся по абзацам, ≤3000 симв., заголовок дублируется в каждый чанк); `id = uuid5("{act}:{article}:{i}")` + версии по hash текста → идемпотентно. Обновление акта = delete-by-filter `act==X` + upsert.
- **Хранение:** ОДНА коллекция + роутинг по полю `act` — стыки (ТК+СК) бесплатны, интерференция ничтожна (MRR −0.005). **Бенчмарк (ключ к защите):** bge-m3 R@5 **0.97** книжный / **0.84** живой; reranker (bge-reranker-v2-m3) и гибрид BM25+dense измерены — **оба НЕ помогают** (обоснованный негативный результат). Eval-наборы в `pipeline/`.
- **Победы над источником:** редакция по **макс. дате**, не max(rdk) — rdk не монотонен (иначе залили бы право 2001 г.); фолбэк `savertf`→`fulltext` (НК ч.2 ~9МБ и короткие законы); **ложный `repealed`** («часть утратила силу» гасила всю статью, в КоАП ~49 живых вкл. 12.9) — починен end-to-end (КоАП 88→39, 0 ложных по базе); двойные дефисы `201.15-2-1`, старый формат 1991 г., голые стабы; **коллизии номеров ФЗ** (115-ФЗ и 218-ФЗ — по два разных закона; старые акты — римская цифра `4462-I`; 255-ФЗ прятался под исходным названием) → каждый `nd` подтверждается реальным `fetch+parse`, не выдачей поиска.
- **Волна 3 (07-13, залита):** +18 актов / +2009 чанков — микрофинансы, животные, платёжная система, ПОД/ФТ, полиция, призыв, больничные, самозанятые, кредитные истории, инвалиды, ВК РФ, туризм, нотариат, информация, реклама, соцпомощь, ветераны, регистрация ИП. Автопоиск `nd`: ИПС-параметр `a1` (по названию) + пагинация `start=`.
- **Аудит зоны (07-13): 57/57 чисто** — каждый акт через реальные fetch+parse+chunk по 5 критериям (парсинг / ложный repealed / свежесть редакций / запас min_articles / пустые active — всё по нулям, прод актуален).
- **Автообновление — код готов, проверен вживую:** двухъярусный `pipeline/watch.py` — ярус 1 (каждую ночь, дёшево): лёгкая карточка редакции по всем актам против базовой линии; ярус 2 (редко, дорого): полный `run` только для изменившихся → обычная ночь ≈ 2–3 мин. Guard: редакции с датой вступления в будущем пропускаются. Ретраи на уровне акта (ИПС при троттлинге кидает `FetchError`). Airflow DAG: `pipeline/dags/law_update_dag.py` (fan-out, `max_active_tasks=1` — эмбеддинги на CPU). **Развёртывание (Роль 4), строго А→Б→В:**
  - **А. Volume для `.state`** (иначе пересборка обнуляет хэши → полный ре-эмбед; пересборка образа не нужна, только `docker compose up -d bot`):
    ```yaml
    services:
      bot:
        volumes:
          - pipeline_state:/app/pipeline/.state   # ← добавить
    volumes:
      pipeline_state:                             # ← добавить
    ```
  - **Б. Базовая линия** (один раз, после А): `python -m pipeline.watch --seed`
  - **В. Расписание:** Airflow **или** cron: `0 4 * * * cd /app && python -m pipeline.watch >> /var/log/law_watch.log 2>&1`
- Хвост (не блокирует): ИПС-`nd` для 138-ФЗ (гражданство 2023; найденный вёл на утративший силу 62-ФЗ).

## Роль 4 · Infra (`infra/`, `shared/`, корень) — ветка `Vitaliy_Svs`
- Скелет репо, `shared/contracts.py` (ратифицированы: `UserDocument`, `doc_ids`, `ingest_document(filename)`, `Citation.text`), docker-compose (bot+qdrant+postgres, порты только 127.0.0.1), миграции, Dockerfile (tesseract-ocr-rus), фикс упаковки подпакетов (`packages.find`).
- **`search_law(query, user_id, acts=None, doc_ids=None)`** — серверные `MatchAny`-фильтры + payload-индексы (`act`, `user_id`/`doc_id`); `shared/embeddings.py` (bge-m3, 1024, без префикса).
- **Egress:** контейнер xray-proxy (хостер РФ режет api.telegram.org). ⚠️ Личная VPN-подписка — нужно постоянное решение (в открытых задачах).
- **Деплой-дисциплина (выработана за проект):** маркеры фич проверяются в собранном образе **ДО** деплоя (паттерн «стухшей сборки» ловился многократно); длинные заливки — `nohup`+`disown` внутри контейнера (переживают обрывы SSH); перед сборкой `docker image prune` (диск однажды доходил до 99%); после заливки — точный пересчёт точек по Qdrant, не по логам.
- **Все волны данных залиты** (Tier-2 +11, Tier-3 +5, бытовые +20, волна 3 +18) → **15 809 точек**; полный переиндекс `--force` 39/39 (фикс ложного repealed). Все фичи Ролей 1/2 в проде, 0 рестартов за всю историю деплоев.
- **Автообновление НПА задеплоено (07-14, шаги А→Б→В из чек-листа Роли 3):** volume `pipeline_state:/app/pipeline/.state` в docker-compose (аддитивно, без пересборки образа — `docker compose up -d bot`, 0 рестартов) → внутри контейнера `python -m pipeline.watch --seed`, все 57 кодов пайплайна получили базовую линию редакции → расписание на хосте (cron, Airflow не поднят на сервере): `0 4 * * * cd ~/pocket-law-ai && docker compose exec -T bot python -m pipeline.watch >> ~/pocket-law-ai/law_watch.log 2>&1` (`/var/log` под `dev4` без прав записи — лог увёл в домашний каталог). С сегодняшней ночи сверка редакций автоматическая, ручной реиндекс больше не единственный путь обновления базы.
- **Пересборка образа (07-14) — ловля «стухшей сборки»:** предыдущий деплой (`up -d` под volume автообновления) пересоздал контейнер, но НЕ пересобрал образ — бот оставался на сборке от 07-13 15:22, без 18 коммитов Роли 2 (собака→498-ФЗ, температура по узлам, реальный eval, выравнивание цитат/uncovered-гейт). Поймано по чужому флагу, не самостоятельно — на будущее держать в голове, что `up -d` без `--build` не подхватывает код. Пересобрал `docker compose build bot` с main `268cce8`, маркеры (`UNCOVERED_BRANCHES`, `AGENT_INTENT_TEMPERATURE`/`HYDE_TEMPERATURE`, keyword-net `собак`/`498`, `_align_citations`) подтверждены в образе ДО деплоя, задеплоил, 0 рестартов.
- **Хвосты аудита Роли 3 закрыты (07-14):** (1) `.env.example` синхронизирован с кодом — +11 ручек (LLM/агент/STT/eval) секциями, устаревший комментарий поправлен, помечено «дефолты в agent/config.py»; (2) мёртвый `make fixtures` (каталог `agent/fixtures/` удалён) убран из Makefile, quick-start в README/CLAUDE/ADVOCAT переведён на наполнение через `pipeline.run --act`; (3) `Agent.transcribe_voice` внесён в `contracts.py` — но это ×4-файл, поэтому отдельной веткой `role4-contract-transcribe-voice` под PR-ратификацию, а не прямо в main (сигнатура 1-в-1 с реализацией, аддитивно). Файлы Роли 4 (корень/доки) — прямо в main; настройка деплой-ключа сервера см. ниже.
- **Deploy key для пуша с сервера (07-14):** на сервере не было исходящей аутентификации к GitHub (remote был HTTPS без токена → `git push` висел). Завёл repo-scoped SSH deploy key (`~/.ssh/deploy_pocket_law_ai`, write-доступ), remote переключил на обход блокировки порта 22 хостером — `ssh://git@ssh.github.com:443/...` (тот же паттерн, что egress Telegram). Pull/push с сервера проверены end-to-end.
- **Оперзнания:** хостовый venv без torch — пайплайн гонять только в контейнере бота; `AGENT_HYDE=0` — первый кандидат при лимитах Polza (env, без пересборки); агентский `tests/` не попадает в образ (свойство пакетирования, `bot/tests` попадает); ИПС троттлит — разовые `ConnectionReset`/`502` в логе не провал, retry вытягивает; **⚠️ в панели Selectel НЕ трогать роутер подсети `192.168.0.0/24` / floating-IP `89.223.29.157`** — на нём и SSH-вход, и egress бота (после чистки лишних роутеров 07-10 проверено: живы).
