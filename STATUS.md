# STATUS — живой журнал команды

> Единственный источник правды о том, кто что сделал. Тезисно. Подробная история — в git-логе.
> Сжат 2026-07-08 по общему согласию (было 551 строка поэтапного лога).

## Правила
1. **Начало сессии:** `git pull origin main`, дать Claude прочитать этот файл.
2. **Конец блока:** правишь **только свою** секцию (владение из TEAM_PLAN). Коммит `status: роль N` — **можно сразу в main без PR** (журнал, секции не пересекаются). Пуш отбило → `git pull --rebase origin main`.

---

## 🟢 Текущее состояние системы (MVP собран)
- **Бот ЖИВОЙ в Telegram** (@pocket_law_ai_bot) в docker, реальный агент, egress через xray-прокси Роли 4, `STORAGE_BACKEND=postgres`. И0–И3 закрыты.
- **Данные:** ТК РФ, `law_articles_dev` = 623 чанка / 1024-мер (`deepvk/USER-bge-m3`), `source_url` в payload → кликабельные цитаты.
- **LLM:** Polza.ai, `google/gemini-2.5-flash-lite` (быстрая/дешёвая), абстрагирована (смена = `LLM_MODEL`), ретраи на 503.
- **Итоговая метрика к защите: сквозной Recall@цитаты 95% (59/62)**, anti-hallucination подтверждён.

### Открытые хвосты (не блокируют MVP)
- **Роль 4:** Branch protection на `main` (задача 10). Постоянное egress-решение вместо личного VPN.
- ~~**Роль 2 (UX — от Роли 1):** off-topic → переспрос про сферу права~~ **СДЕЛАНО** (2026-07-08): не-юр вопрос (погода/рецепт/код) → мягкий отказ по области (`make_offtopic`, refused=True); пустой ввод → уточнение. Заодно **расширил `is_legal`** в intent (развязал с branches) — реальные юр-вопросы (возврат товара, развод, сосед, штраф) больше не отшиваются как «не моя тема»: `is_legal=True` + верный роутинг по кодексу, честный отказ «нет норм в базе» до заливки соотв. кодекса. Проверено вживую. В `Roman_SPT`.
- **Пользователь:** лимит трат на аккаунте Polza (только владелец ключа).
- **Фаза 2 (вне MVP):** LLM query expansion уже сделан; тезаурус/граф статей, другие кодексы, `law_versions`.

---

## Роль 1 · Bot Layer (`bot/`) — ветка `Dordzhi-Msk` · **DoD закрыт**
- aiogram 3.x (long polling): `/start` (обяз. согласие 152-ФЗ, без него бот молчит), `/help`, `/delete_my_data`. FSM + 3 ветки контракта (ответ с цитатой / уточнение / отказ).
- Приём **PDF/фото** (валидация по magic-bytes, ≤20 МБ) → `ingest_document`; **детект URL** → `ingest_url`; ссылка+вопрос в одном сообщении.
- Форматтер 3.5: MarkdownV2 с экранированием, «Основание: ст. N», дисклеймер, разбивка >4096, кликабельные `source_url`, мягкая деградация на плохой разметке.
- **`PostgresRepository`** (`STORAGE_BACKEND=postgres`): согласие/история/документы переживают рестарт (критично для 152-ФЗ); rate-limit поверх `dialog_history`; глобальный `@dp.errors`; edited_message; идемпотентное согласие.
- Бот подтверждён live в Telegram на реальном агенте. Правок под баг «документ не доходил до ответа» в `bot/` не потребовалось (баг был в агенте).
- После пересборки образа (PR #21) сценарий документ→ответ **подтверждён в живом Telegram**: ссылка ст.115 → «28 календарных дней» (ст.115/267/322); полный ТК → вопрос про увольнение → grounded-ответ (ст.81/180/296/318/394); **загрузка PDF-договора** → «какой оклад?» → «120 000 ₽» (число есть только в файле, не в ТК → доказано, что документ прочитан) + ст.57. Документ реально влияет на ответ, цитаты кликабельны. **Все задачи Роли 1 закрыты.**
- **UX-фикс (PR #22, смержен):** вопрос, посланный ДО «документ принят», при конкурентной обработке апдейтов уходил в агента раньше индексации → ответ без контекста. Теперь на время приёма юзер помечается в `_ingesting`, а `on_question`/`on_edited_question` отвечают мягкой подсказкой «⏳ обрабатываю документ, задайте вопрос после подтверждения» (агент не зовётся); метка снимается в `finally`. +3 теста (25 passed). **Роль 4: подхватится следующей пересборкой образа.**

## Роль 2 · Agent Orchestrator (`agent/`) — ветка `Roman_SPT` · **функционально завершена**
- Граф LangGraph `intent → retrieve → verify → {compose | clarify | refuse}`, API 3.1 (`Agent()` — drop-in для бота).
- **LLM абстрагирован** (Polza.ai, httpx, без новых зависимостей); модель `gemini-2.5-flash-lite` (замер: 4.8с vs 12.7с deepseek); **ретраи на транзиентный 503/сеть** (до 3 попыток, 4xx — наружу).
- **Anti-hallucination:** цитаты только из `verify`; `INSUFFICIENT` → честный отказ; неюр. вопрос → уточнение. **Безопасность промптов** (инъекции через файл/URL, социнженерия «создатель/президент») — проверено на живом LLM, не течёт.
- `ingest_document` (PyMuPDF + OCR tesseract rus), `ingest_url` (**SSRF-защита**: http/https, блок приватных/loopback IP, лимит 20 МБ); изоляция `user_documents` по `user_id`; `source_url` → `Citation`.
- **Переформулировка «житейский → юридический»** перед ретривом (без доп. вызова LLM): живой R@5 **84→91%**, R@1 56→66%.
- Лимиты бюджета: `max_tokens=900`, `recursion_limit=8`, обрезка вопроса 4000, guard пустого ввода. `confidence_log` в Postgres (graceful).
- **Фикс (PR #21, смержен):** загруженный документ/URL не доходил до ответа — причины: `embed_passages` индексировал старой e5 (768) в коллекцию bge-m3 (1024) → upsert падал; `retrieve` срезал `user_doc`-чанки; мета-фраза «в этом документе» сбивала intent. Исправлено (embed из `shared.embeddings`, retrieve не режет документ, intent чистит мета-отсылку) — проверено вживую (PDF и реальный URL).
- **Полная сквозная проверка ✅** (все ветки графа, инъекции, изоляция, пустой ввод) + **итоговый Recall 95% (59/62)** на боевом стеке.
- **Мультикодекс — наша сторона готова (навстречу плану Роли 3 от 2026-07-08):** `intent` уже возвращает предполагаемые акты (поле `branches` → canonical `act` через `config.BRANCH_TO_ACTS`; стык кодексов → несколько; не уверен → []). `retrieve` мягко фильтрует выдачу по `act` с фолбэком на все (recall не теряем). Живо на flash-lite: «кража»→[УК РФ], «развод»→[СК РФ, ГК РФ], «уволить»→[ТК РФ]. 35 юнит-тестов зелёные (+7). В `Roman_SPT`.
  - **Роль 4:** как выкатите `search_law(query, user_id, acts)` — переключим ретрив с клиентского фильтра на серверный (пока фильтруем поверх top-K). Уточните: параметр = `acts: list[str] | None` (MatchAny)?
  - **Роль 3:** СК активирован — подтвердите, что строка `act` в payload = **`СК РФ`** (ровно так в `config.BRANCH_TO_ACTS`, иначе фильтр уйдёт в фолбэк). Для **ЗоЗПП** (не кодекс) дайте каноничную строку `act` — добавлю отдельной отраслью.

## Роль 3 · Data Pipeline (`pipeline/`) — ветка `Roma_MSK` · **DoD закрыт**
- `fetch → parse → chunk → embed → upsert`, версионирование по hash (без дублей), CLI `python -m pipeline.run`. Источник — ИПС pravo.gov.ru (двухшаговый экспорт последней редакции). ТК РФ: 538 статей → 623 чанка.
- **Бенчмарк эмбеддингов → базовая модель `deepvk/USER-bge-m3`** (1024, без префикса): R@5 «книжный» **0.97**, «живой» **0.84** (лексический разрыв — люди не пишут юр-терминами). e5-base/large отмели по цифрам.
- **reranker (bge-reranker-v2-m3) и гибрид BM25+dense измерены — оба НЕ помогают** (reranker ронял ст. 81; гибрид MRR 0.89→0.70). Для MVP — чистый dense. Обоснованный негативный результат для защиты.
- `source_url` в payload (устойчивая ссылка на акт целиком; анкоров на статью у ИПС нет). Принцип юр-eval: метка = **множество** приемлемых статей.
- Рычаг на оставшийся зазор — **LLM query expansion** (зона Роли 2, реализован). Наборы `eval_colloquial.json` (32 «живых») и `eval_extended.json` (138 «книжных»; +18 Роли 2 = 156 в книжном прогоне) — в `pipeline/`.

### 2026-07-08 — план мульти-акт (Tier 1: ЗоЗПП + СК РФ) · решение по хранению
**Решение: ОДНА коллекция `law_articles`, роутинг по полю `act` — НЕ режем на коллекции-на-закон.** Почему так эффективнее агенту: при отдельных коллекциях агент обязан выбрать базу ДО поиска, ошибка отрасли = жёсткий промах; при одной — «не уверен» = ищем без фильтра по всем, нужный закон всплывает сам (косинусы bge-m3 сравнимы между актами, одна модель + normalize). Мягкая деградация вместо промаха, стыковые вопросы (ТК+СК) бесплатны, инфра минимальна. Единственный плюс раздельных коллекций (своя модель/чанкинг на закон) нам не нужен — всё русский юртекст.

Прод-схема:
- **Хранение:** один `law_articles`, bge-m3/1024, payload уже с `act`. `id` чанка уже namespace'ится по акту (`uuid5("{act}:{article}:{i}")`) — коллизий номеров между кодексами нет ✓.
- **Роутинг (агент):** `intent` возвращает вероятные акты `list[str] | None` → `search_law(query, acts)` фильтрует `MatchAny(act)`, когда уверен; иначе без фильтра (recall не теряем). Стык законов → несколько актов.
- **Быстрый путь по номеру:** явная ссылка «статья N <кодекс>» → прямой lookup по (`act`, `article_no`), минуя семантику.

**Что делаем (Роль 3):** переиндекс одного акта через **delete-by-filter `act==X`** + upsert (обновление одного закона не трогает остальные, снятые статьи чистятся). Запись `act` уже есть. Перед активацией — spike на парсинг ЗоЗПП/СК (структура HTML ИПС может отличаться от ТК). Черновик `ACTS_DRAFT` в `pipeline/acts.py` закомментирован, nd не заполнены (сверить с pravo.gov.ru), в бота НЕ заливаем.

**Важно другим:**
- **Роль 4:** payload-индекс на `act` (и `article_no`) в `init_qdrant`; опциональный параметр `act` в `search_law` (аддитивно — без него всё как сейчас).
- **Роль 2:** `intent` отдаёт предполагаемый `act`(ы) → проброс в `search_law`; не уверен → `None`. Опционально — быстрый lookup по явному номеру статьи.

### 2026-07-08 — СК РФ провалидирован локально, готов к боевому прогону; ЗоЗПП ждёт твик парсера
**СК РФ (nd `102038925`) — dev-прогон end-to-end пройден.** fetch→parse→chunk→embed→upsert в локальную `law_articles_dev`: **177 статей → 183 чанка**, ред. 15.05.2026, payload с `act="СК РФ"` + `source_url`. Поиск точный: «как подать на развод» → ст. 22/21/23, «алименты сколько %» → ст. 81 СК. Активирован в `ACTS` (`--act sk_rf`); `--act` обязателен → штатный реиндекс ТК его НЕ подхватывает (безопасно).
**Межзаконная интерференция измерена — ничтожна** (ТК-only vs ТК+СК без фильтра, наши 138+32): R@5 не меняется (0.971/0.844), MRR −0.005 книжный / 0 живой. Единственный стык — «семья» (ст. 264 ТК опекуны 1→4). Вывод: `act`-фильтр — точечное улучшение, **не блокер**; одна коллекция безопасна.
**ЗоЗПП (nd `102014512`) — готов.** Парсер доработан под ФЗ: голые стабы утративших силу статей (в ЗоЗПП ст. 26/38/42 — просто «Статья N.» без заголовка и текста, а не «(Утратила силу…)» как в ТК) → `repealed`. Локальный dev-прогон: 55 ст. → 75 чанков; поиск по потребительским вопросам находит кластер (ст. 12/18/25). Активирован в `ACTS`. **Побочно** фикс корректно переклассифицировал 5 голых стабов ТК (7/85/98/175/360) active→repealed — **индекс ТК не меняется** (они пустые, 0 чанков; 538 ст./623 чанка как было).
**Полная интерференция ТК+СК+ЗоЗПП измерена — ничтожна:** R@5 без изменений (0.971/0.844), MRR −0.005 книжный / 0 живой. ЗоЗПП поверх СК добавил ноль интерференции (потребительское право лексически далеко от трудового).

**Важно другим:**
- **Роль 4:** СК и ЗоЗПП готовы к боевому прогону на сервере — `python -m pipeline.run --act sk_rf` и `python -m pipeline.run --act zpp_rf` (та же `law_articles_dev`, +183 и +75 точек к ТК, размерность 1024 та же, ТК не трогают). По желанию — payload-индекс на `act` (точечная польза на стыке «семья»).
- **Роль 2 (ответ на ваш вопрос по строкам `act`):** сверил по payload, что реально пишет пайплайн — **СК = `СК РФ`** (ровно так, ваш `BRANCH_TO_ACTS` совпадёт, фолбэка не будет ✓). **ЗоЗПП = `ЗоЗПП`** — каноничная строка `act` для потребительского права (это ФЗ, не кодекс; добавляйте отдельной отраслью). Обе строки берутся из `ActInfo.act` в `pipeline/acts.py`, менять их не буду без согласования. Отлично, что роутинг у вас уже готов — после боевого прогона Роли 4 связка заработает end-to-end.

## Роль 4 · Инфраструктура (`infra/`, `shared/`, корень) — ветка `Vitaliy_Svs`
- Скелет репо, `shared/contracts.py` (раздел 3 TEAM_PLAN 1-в-1), `docker-compose` (qdrant + postgres:16 + bot), миграции 3.4, `init_qdrant.py`, `Dockerfile` (tesseract-ocr-rus), `pyproject.toml` (pymupdf/pytesseract/pillow/aiohttp-socks). Фикс flat-layout сборки.
- `search_law()` (гибрид: `law_articles`/`_dev` + `user_documents` по `user_id`, `TOP_K_LAW=10`); `shared/embeddings.py` — префикс по модели (bge-m3 без префикса), `EMBED_DIM=1024`; `init_qdrant` пересоздаёт коллекцию при несовпадении размерности. `source_url` в `RetrievedChunk`/`search_law`.
- **Telegram-egress:** блок хостера (Selectel/РФ) обойдён контейнером `xray-proxy` (порты на `127.0.0.1` для всех dev-пользователей). ⚠️ Личная VPN-подписка, не командная инфра — нужно постоянное решение.
- **Докер-бот поднят и стабилен** (`STORAGE_BACKEND=postgres`, прокси, LLM-ключ в общем `.env`); реиндекс `--force` → `law_articles_dev` 623/1024 с `source_url`. И3 закрыта.
- Прод-`.env`: `LLM_API_KEY` + `LLM_BASE_URL=https://api.polza.ai/api/v1` + `LLM_MODEL` + `EMBED_MODEL=deepvk/USER-bge-m3`/`EMBED_DIM=1024` заданы. Секреты — только на сервере (не в git).
- **2026-07-08:** пересобрал образ бота под PR #21 (Роль 2). Нюанс: на момент первой попытки PR ещё не был реально смёржен, хотя в старом логе значилось «в main» (`git merge-base --is-ancestor` = `NO`) — дождался фактического мержа Ромой, тогда пересобрал (`--no-cache` по факту, не по кэшу). **Проверено живым вызовом `agent.Agent()` в контейнере**, не только по коду: `ingest_document` с настоящим PDF → `ok=True`; `answer_question` по содержимому документа ответил верно («испытательный срок три месяца»). Бот стабилен, 0 рестартов.