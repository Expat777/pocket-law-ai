[pytest]
# Локальный конфиг тестов Bot Layer. После И0 может переехать в корневой
# pyproject.toml Роли 4. asyncio_mode=auto — не требуем маркер на каждом async-тесте.
asyncio_mode = auto
testpaths = tests
